# 行程查开发约定

BreezeTravel 是行程查唯一产品主仓。TripCheck 仅供只读能力参考，不运行第二套产品。

## 当前方向

本次重建由项目所有者批准，替代旧 G01–G07 与候选交付流程。只维护三个事实入口：

- [产品定义](docs/product/PROJECT_CHARTER.md)：用户需求、行为和技术边界。
- [实施计划](docs/governance/IMPLEMENTATION_PLAN.md)：顺序与验收。
- [当前状态](docs/governance/CURRENT_GOAL.md)：已完成、未完成、实际验证及下一步。

其他指导、历史 ADR、旧工作包、旧基准只作参考；冲突时以本次用户授权和这三个入口为准。

## 实施规则

1. 先查看工作树并保护已有修改；在隔离工作树开发。禁止删除旧业务数据、改写历史证据、强推、合入 main 或未经授权公开部署。
2. 每次交付完成一条可操作的用户路径。先对齐接口，前后端可按互不重叠的文件并行，主任务串行集成；短期子代理可实现、验证和复核，不要求另建用户任务。
3. PostgreSQL 保存所有权、版本、幂等及业务状态；Redis 仅缓存、限流及可重建状态。复用 v3 API、现有 worker 和模块化单体；不为了技术名称新增服务。
4. 模型理解语义，服务端验证结构、地点、时间和路线事实。不能虚构地点、坐标、交通、价格或成功状态。地点错误、隐私泄露、删除失败、并发覆盖、编辑自动重算路线必须修复。
5. 所有变更和撤销生成新版本；使用不透明 ETag 与幂等键。匿名秘密只放 HttpOnly cookie。日志不记录原文、令牌、密钥或敏感路径标识。
6. 检查用户看到的结果，运行与修改相关的单测、数据库和浏览器验证；对未运行的真实服务或真人体验如实记录。固定回放不算模型效果。
7. 更新当前状态并保存可回退的本地 checkpoint。方向切换后不继续建设签名、候选绑定、重复审查、盲测或治理平台。机器一致性检查不代表产品通过。
8. 两轮不同修复无改善时缩小复现并调整方法；可以保留明确的局部待确认，不能通过静默遗漏、假成功或削弱安全测试取得通过。

普通用户页面使用简洁中文，不暴露内部阶段、模型、Provider、hash、revision、receipt、source span 或诊断数据。界面与交互要求见产品定义。

历史指导原件在 [归档](docs/governance/archive/pre-convergence-20260905/INDEX.md)。原始工作区及其未提交修改保持不动。
