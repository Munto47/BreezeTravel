fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# 数据库
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.35
psycopg2-binary==2.9.9
pgvector==0.3.5

# LangChain & LangGraph
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.13
langgraph>=0.2.0

# OpenAI SDK（RAG Embedding + LLM 调用，兼容 DeepSeek / SiliconFlow）
openai>=1.0.0
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.1.0

# 路线优化算法
numpy>=1.26.0
scikit-learn>=1.3.0

# 和风天气 JWT 认证（EdDSA/Ed25519）
PyJWT[crypto]>=2.8.0
cryptography>=42.0.0

# HTTP 客户端
aiohttp==3.10.10
httpx==0.27.2

# 缓存
redis==5.1.1

# 工具
python-dotenv==1.0.1
nanoid==2.0.0

# ── Advanced RAG（Sprint 1 新增）────────────────────────────────────────────

# BM25 中文分词（Sparse 检索 / tsvector 入库）
jieba>=0.42.1

# Cross-Encoder 重排序（bge-reranker-v2-m3）
# 需要 GPU 推理，已有 torch 环境则自动使用 CUDA
# 注意：FlagEmbedding 会自动安装 torch，Docker 镜像会因此增大约 2GB
# 若需轻量部署，可注释掉并设置 RERANKER_ENABLED=false
FlagEmbedding>=1.2.5

# RAG 评估框架（RAGAS）
ragas>=0.2.0
datasets>=2.0.0
