fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# 数据库
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.35
pgvector==0.3.5
psycopg[binary]>=3.1.0
langgraph-checkpoint-postgres>=2.0.0

# LangChain & LangGraph
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.13
langgraph>=0.2.0

# OpenAI SDK（RAG Embedding + LLM 调用，兼容 DeepSeek / SiliconFlow）
openai>=1.0.0

# 路线优化算法
numpy>=1.26.0
scikit-learn>=1.3.0

# 和风天气 JWT 认证（EdDSA/Ed25519）
PyJWT[crypto]>=2.8.0
cryptography>=42.0.0

# HTTP 客户端
aiohttp==3.10.10
httpx==0.27.2

# 缓存
redis==5.1.1

# 工具
python-dotenv==1.0.1
nanoid==2.0.0

# Sprint 4：用户体系 / 短信
alibabacloud-dysmsapi20170525>=2.0.0
jinja2>=3.1.4

# Advanced RAG：BM25 中文分词
jieba>=0.42.1

# Cross-Encoder 重排序（可选，运行时按需 import，缺失自动降级）
# 本地开发/GPU 机器安装：pip install FlagEmbedding
# Docker 轻量部署不安装，RERANKER_ENABLED 默认 false
# FlagEmbedding>=1.2.5

# RAG 评估（仅开发环境，非运行时依赖）
# pip install ragas datasets
