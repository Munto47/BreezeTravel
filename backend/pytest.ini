[pytest]
testpaths = tests
timeout = 60
markers =
    unit: deterministic test without network or database
    integration: controlled PostgreSQL Redis or application integration
    local_e2e: local full-stack API or browser flow
    external: GPU LLM external API or public network validation
filterwarnings =
    ignore:pkg_resources is deprecated as an API:UserWarning
