# 当前状态：外侧回环与就近确认已交付本地体验

目标 `TC-DUAL-MODE-HORIZONTAL-R0` 仍为 `IN_PROGRESS`。本轮仅交付所有者 2026-09-06 第四轮界面反馈，不声明整体 R0/R1/R2 或新版 G07 通过。

## 基线与保护

- 分支：`codex/soft-canvas-owner-feedback`。
- 工作树：`D:/CODEX/BreezeTravel-soft-canvas`。
- 本轮精确起点：`a91d86f`；旧重力链与回环证据是历史，不拼接本轮结论。
- 只保存本地 checkpoint，不推送、不发布、不合并 main。
- 原工作树、数据库、密钥与用户已有修改保持不动。公共 API、数据库和 Provider 配置未改变。

## 本轮用户结果

- 第一行左到右、第二行右到左。两行成对布局，交通曲线及摘要在第一行上方、外侧折返处和第二行下方，不穿过两行中间。更多行继续成对排列，手机两列，无横向滚动条。PNG 共用坐标和方向。
- 保留拖拽占位预览、让位和跨日期操作，移除蓝色插入竖条；反向行和跨日期落点按新坐标换算。取消零写入、确认单次写入、撤销和失败恢复保留。
- 待确认地点点击后在卡片旁展开非模态下拉框，输入地点名称、显式搜索、选择候选并确认。打开和仅选择不写入、不自动搜索；失败就在原处重试，关闭／Escape 恢复焦点。不接受浏览器坐标。
- 当前真实路线优先显示步行分钟数和距离；步行不可用才使用既有可用交通结果。过期、缺失或无法绑定到相邻地点的路线不冒充当前事实。
- 卡片页直接提供“更新步行路线”，复用既有手动更新接口和写锁。编辑不自动计算路线，过期路段仍需用户点击更新，不通过估算补数字。
- 生成页纸片、地点、粒子和旋转动画放慢；四条简短提示每九秒切换，悬停或聚焦可停留阅读，也能手动切换。减少动态效果设置下不自动轮播。真实阶段仍只由服务端进度推进，不延迟完成、不伪造进度。
- 卡片仅表达相对顺序；地图、住宿、隐私、分享与导出能力保持，不恢复绝对到访时间要求。

## 实际验证

`AUTOMATED_TEST`：

- 最终完整浏览器组合：**108 passed、5 skipped**。跳过项都是此前已有的 `RETIRED_CHECKS_PAGE`，本轮未新增跳过。
- 新增 11 条测试：四视口外侧连线（1440、1280、390、360px）；三视口下拉框就近、无模态、显式搜索与一次确认；步行优先和真实距离、过期隐藏旧时间及点击才更新；九秒提示／悬停／手动分页／减少动画；失败关闭及焦点恢复；拖拽占位保留但竖条透明。
- 既有 SSE 首连／续订／停止／轮询降级、跨日鼠标／触摸拖动、撤销／并发、隐私、地图边界、协同入口和完整 PNG 均在该组合中复跑。
- 初测发现折返标签换行后越出两行间的预留位置，以及失败搜索按钮失焦后 Escape 无法关闭。分别固定标签宽度／紧凑行高、增加局部下拉框存续期间的 Escape 处理后，11 条定向与完整组合均通过。未删除安全断言。
- TypeScript 检查、Next.js 生产构建与本地服务重启通过；桌面、手机下拉框、生成提示及真实照片路线截图已人工式视觉复核（不是所有者真人验收）。
- 机器一致性检查 PASS（CONTRACT_ONLY），只验证文档合同，不代替产品验收。
- 本轮未修改后端，后端完整测试、40 条语义样例及旧候选统计未重跑。

`LIVE_PROVIDER_EVIDENCE`：

- 最终生产构建实时复测 **2/2 passed**：真实文字、事件流、POI 图片与全日期地图；另一路六地点北京攻略验证桌面折返、手机两列和相对顺序。
- 实际截图显示首段步行 **38 分钟／2.8 公里**，其他路段显示对应服务端距离与耗时；右侧转弯标签正常换行，无横向溢出。
- 只使用合成攻略及现有开发授权。截图保存在忽略的 `frontend/test-results-live-soft`，不提交原文、凭据或临时资源标识。
- 这两条不外推三城、协同驾车、真人易用性或整体 R1。

## 入口、边界和下一步

本地入口：**http://127.0.0.1:3118**。API 8018、Yjs 1246、PostgreSQL 55451、Redis 56401；数据库与匿名草稿保留，已重新构建当前源码。

Safari、实体手机、真实双用户协同／驾车、完整候选矩阵及所有者三份真实攻略验收：NOT_RUN。照片和路线以实际服务结果为准；暂无路线时不虚构步行距离或时间。长攻略仍需要纵向浏览。

本轮代码、测试和状态在同一本地 checkpoint 保存。下一步是所有者体验当前页面；有实际反馈后继续自主修复阻碍使用的问题，无需所有者排查日志或运行命令。
