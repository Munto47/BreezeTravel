# 当前状态

更新：2026-09-05。目标 `TC-EXPERIENCE-V1`，状态 `COMPLETED`，完成范围为阶段 0–2 的本地完整体验版。阶段 3–5 未标记完成。唯一产品定义为 [产品定义](../product/PROJECT_CHARTER.md)，顺序与验收为 [实施计划](IMPLEMENTATION_PLAN.md)。

## 保护与方向

- BreezeTravel 为唯一产品主仓；TripCheck 为只读供体。
- 原 BreezeTravel 提交 `6f1337300fd62950857786eee0ca41a8b196a832`，原 TripCheck 提交 `01bc0ad65cbb1dc4f8fb4db2cabb6ff9211e3681` 保留。
- 新工作树 `D:/CODEX/BreezeTravel-experience-v1`，分支 `codex/experience-v1`；供体交接工作树 `D:/CODEX/TripCheck-donor-handoff`。
- 原 BreezeTravel 未提交 AGENTS 修改未触碰，并备份到 `D:/CODEX/rebuild-checkpoints/20260905`。
- 旧目标、工作包与签名／blind／候选限制均为 `SUPERSEDED`；原历史结果不改写，不转为通过。原指导见[归档](archive/pre-convergence-20260905/INDEX.md)。

## 已完成与当前切片

阶段 0–2 已完成。方向切换提交 `bb789b9`，供体交接提交 `c1b5807`；本文件与通过验收的产品实现一起保存为本地交付 checkpoint。没有推送、合并 main 或公开部署。

- 首页匿名真实输入与固定示例分开。真实输入直接使用已授权 Qwen，模型负责日期、顺序、取消、备选与时间语义，服务端验证结构和地点；真实链不再通过旧中文词根规则重写语义，也不回退成固定示例。
- 新桌面纵向行程与真实高德地图双向联动；手机切换行程／地图。地点详情支持真实候选确认、时间与停留编辑、移动和删除；单地点、缺时和局部不可用保留操作。
- 时间检查使用前站结束或开始加停留，再加真实交通。预约／锁定不自动顺延；建议先预览，再以新版本原子采纳；旧预览拒绝采纳。编辑后路线标记需更新，由用户手动重算并复检；迟到结果不覆盖新版本。
- 撤销生成新版本；刷新恢复编辑。邮箱注册／登录后领取当前行程和修改，同会话其他匿名稿仍可访问，保存示例继续显示固定回放标识。
- 手动删除原文清除原文、引用和未公开内容，保留已展示卡片、地点绑定与撤销所需的脱敏历史；整程删除清理派生数据。每份匿名稿 24 小时，账户创建或领取起默认 30 天；新建稿和幂等重放不延长旧稿期限。
- 统一脚本运行 Web、API、必要 worker、PostgreSQL 与 Redis 兼容缓存；默认先构建当前源码与浏览器配置，再启动页面，构建失败不启动旧包。复用现有 JSON、版本与 34 条历史迁移，没有重建旧业务库或新增第二套 API。旧房间与冻结功能不进入新后端入口。

运行与备份恢复见[运行说明](../EXPERIENCE_RUNTIME.md)。默认本地地址为 `http://127.0.0.1:3106`。原仓与新运行数据分离，密钥、用户数据、截图与日志留在忽略目录。

## 实际验证

以下只证明本次实现与明确执行的场景，不沿用历史 PASS，也不代表统计效果或真人验收。

| 验证 | 实际结果与范围 |
|---|---|
| 合同与指导 | 两个合同 CLI PASS，仅 `CONTRACT_ONLY`；26 项合同测试包含在下列后端套件中 |
| 当前后端检查 | 203 passed，包含 12 条三城正常／歧义／时间冲突／编辑恢复 HTTP 任务；模型、地点和路线为受控测试输入 |
| 持久化与隐私 | 37 passed，无跳过，包含真实 PostgreSQL、并发、迟到任务、候选、原文删除与整程到期；旧稿到期后不能借新稿延长的会话领取续期。未来时间只用于临时库 |
| 真实服务 | 三城各一份新编写的非固定示例，真实 Qwen＋高德候选＋路线、预览、采纳、更新复检、撤销、整程删除均 PASS |
| 浏览器 | 默认构建后启动方式下完整 12/12 PASS，37.6 秒，0 跳过、0 不稳定重试；含 1440、1280、390、360px、编辑恢复、丢失响应后幂等重试、预览采纳、删除原文、邮箱保存退出重登；其中 2 项明确标为真实高德底图／候选，其余为受控行程任务 |
| 构建与类型 | 当前浏览器配置下生产构建 PASS，共享客户端类型检查 PASS，变更 Python Ruff 与差异检查 PASS |
| 初始化与恢复 | 临时新库初始化与重复执行均通过，34 条迁移无重复执行且已有数据保留；实际备份恢复到新库成功，旧库保留 |
| CI | 工作流 YAML 解析通过，当前后端与 PostgreSQL 命令本地执行；GitHub 远端工作流未运行 |
| 外部环境 | Docker Compose 实际启动、真人使用和生产运行均 `NOT_RUN` |

三城真实调用的本次观测：

| 城市 | 首次卡片 | 确认的路线 | 原下一站时间 → 采纳后 | 撤销 |
|---|---|---|---|---|
| 北京 | 4.07 秒 | 故宫博物院 → 景山公园，12 分钟 | 11:00 → 12:12 | 恢复 11:00 |
| 上海 | 3.54 秒 | 上海豫园 → 上海城隍庙，5 分钟 | 11:00 → 12:05 | 恢复 11:00 |
| 杭州 | 4.03 秒 | 岳王庙 → 曲院风荷，9 分钟 | 11:00 → 12:09 | 恢复 11:00 |

上述输入主动包含待确认地点，测试实际走了候选查询和选择；不能据此推断自动地点覆盖率。三次观测不是 P50/P95 或准确率评测。内部记录模型用量、耗时、修复调用与失败原因；价格配置缺失时费用为未知，不记成零。真实验证脚本为 `backend/scripts/verify_experience_live.py`，不在 CI 自动调用外部服务。

本地原始结果位于 `.local-artifacts/experience/`：`product-backend.xml`、`product-postgres.xml`、`live-three-cities.json`、`browser-production-report.json` 与 `screenshots/`。浏览器临时结果位于 `frontend/test-results/`，均不提交用户数据。

## 与历史测试的关系

阶段 0 选定旧回归为 172 通过、2 失败，已在原工作树复现，涉及秦岭归日／重复和张家界重复。原测试及失败记录保留；它们调用冻结的确定性语义解析器，退出当前真实模型链的 CI 前置项，不记为通过。新语义链另有原文锚定、取消备选、不完整内容与地点搜索资格验证。

其他旧断言按本次明确合同调整：匿名 FULL 与检查允许进入；领取保留其他匿名稿的 cookie；不确定任务接管报告失败而不生成伪结果；显式复检可更新证据而不修改行程；原文删除保留脱敏地点绑定。对应权限、幂等、版本、删除及不重复外部调用断言保留并增加当前行为测试。

## 剩余与下一自主动作

本地完整体验版验收通过。阶段 3 的 60 条样本、质量／延迟／费用对照尚未开展；自然语言编辑、RAG 与有限顺路优化仍属阶段 4，受控真人试用属阶段 5。它们不阻挡本地完整体验版交付，也不标为已实现。

开发模式曾在完整套件首次访问时发生脚本解析错误，后续访问及两次独立捕获未复现，根因尚未确证；失败 trace 保存在 `cold-start-failure/`，没有改写为通过。默认体验现使用构建后的页面，完整 12 项通过；显式 `-Dev` 仅用于开发，不承诺该历史异常已修复。Docker 与公网运行也没有以本地 PASS 代替验证。

下一开发切片是整理 40 条开发样本及独立的 20 条保留样本，先测完整率、地点错误与需人工修改量，再决定优化对象。无需用户诊断内部问题；新增费用、公开部署和招募仍另行确认。
