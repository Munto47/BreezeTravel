# 当前状态：双模式 R0 所有者体验待验收

目标 `TC-DUAL-MODE-HORIZONTAL-R0`，状态 `IN_PROGRESS`。分支
`codex/dual-mode-horizontal-core`，精确基线 `a43b1a8`，隔离工作树
`D:\CODEX\BreezeTravel-dual-mode-horizontal`。

## 已实现的用户结果

- 默认“行程查”支持匿名文字输入、累计快照、SSE 优先与轮询降级、断线续订、停止整理和可编辑部分结果。
- 结果页提供“行程／地图与住宿／优先检查”三视图横链；拖动只从把手开始，先本地预览，确认后使用同一幂等凭据且逻辑上最多应用一次，旧路线随后只显示需要更新。
- 路线预演只消费当前服务端真实几何；无几何时诚实退化为逐站预演。完整 PNG 由浏览器确定性渲染全部卡片，不依赖地图底图或照片。
- 登录后的“协同规划”恢复创建／加入／最近房间、多人同步、AI 问答、明确点击的驾车排线和保存；固定数据核心闭环已验证，真实双用户与真实驾车仍属于 R1。普通界面不显示内部节点、Provider 或引用 ID。
- 最近一次保存路线可单向转入新的账号私有行程查结果；服务端只从数据库读取并白名单转成文字，坐标、旧耗时、照片、RAG 和旧成功声明不会进入新结果。
- 首页、登录、房间和结果页统一为天空蓝、青蓝与暖白视觉；OCR、截图上传和图片理解保持关闭。

## 当前检查点

这是已通过本地固定数据收口的“自动化 R0 候选”，不是总体 R0 PASS，也不是新版 G07 候选：

- 可恢复的产品实现提交：`ac32b02dea045f0099c156d77258bcc259fc7352`；本状态记录在其后的独立治理提交中，不把自身未知哈希写入自身。
- 40 条文字兼容样例：`40/40 safe`、`37/40 exact`、危险输出 `0`。R0/R1 文字子门达到，R2 的 `40/40 exact` 未达到。
- 后端固定数据与接口回归：固定数据组合 `234 passed, 1 skipped, 10 warnings`；其中唯一跳过的真实 PostgreSQL 集成项随后已在当前隔离数据库单独运行通过。当前改动 Python 文件的 Ruff 与编译检查通过。
- 真实 PostgreSQL：当前树在运行中的隔离数据库完成新增 migration、取消／完成行锁竞争和迟到 worker 防写回定向集成 `1 passed, 1 warning`；测试创建的临时数据库已由用例清理。
- 前端：结果页与渐进生成当前组合 `67/67`（结果页 `64/64`、渐进停止／续订 `3/3`），双模式边界 `18/18`；独立 TypeScript、共享客户端构建和 Next.js 生产构建通过。Next 的编译、类型检查和 14 个页面生成均成功；Windows 工作树复用主仓库 `node_modules` 时仅有 standalone 追踪目录软链接 `EPERM` 警告，进程退出码为 0。
- Yjs 服务：鉴权持久化、确认更新排空与立即重启检查 `10/10`。
- 当前主线合同一致性校验：`PASS (CONTRACT_ONLY)`；合同登记了 12 个核心场景，但未把它们冒充为已执行的产品旅程。`product_delivery=NOT_RUN`，因为三份所有者攻略尚未产生真人体验证据。
- 子 Agent 只完成代码与治理的模拟复核；不计作真人证据。

## 证据等级与剩余工作

- `AUTOMATED_TEST`：上述固定数据、浏览器、客户端构建、Yjs 与当前隔离 PostgreSQL 定向检查通过。
- `LIVE_PROVIDER_EVIDENCE`：`NOT_RUN`。真实 Qwen＋高德行程查、真实协同 AI＋驾车属于 R1，不阻塞 R0 所有者体验。
- `HUMAN_USABILITY`：`NOT_RUN`。总体 R0 仍等待所有者使用三份真实文字攻略完成体验。
- R1、R2 与正式 G07：`NOT_RUN`。`PRODUCTION_EVIDENCE`、发布、部署和 `main` 合并：`NOT_AUTHORIZED`。

已知边界：协同地点采用整份快照持久化，尚无基于版本的 CAS；真实双用户乱序写入可能覆盖较新的地点列表。协同排线能构造时间矩阵，但当前保存结果尚未把真实驾车段投影给普通界面。这两项均以诚实的“路线暂不可用”降级，不阻塞单用户固定数据 R0，但进入 R1 的真实双用户／驾车闭环前必须关闭。另一个标签页在未决编辑期间完成账号领取时，旧资源上的跨标签恢复仍需在 R1 前补强。

标准端口由保留的长攻略工作树占用且本机没有 Docker，因此本目标使用持久化隔离端口启动了完整本地运行时：网页 `http://127.0.0.1:3116`、API `8016`、Yjs `1244`，并使用独立 PostgreSQL、Redis 与匿名凭据 Cookie 名。五项服务状态均为运行中，API／Yjs 健康检查和生产首页 HTTP 回读为 200；连续两次应用层重启均以当前源码重新生产构建网页，并成功回收、恢复网页、API 与 Yjs。PostgreSQL 和 Redis 按该重启模式的设计保持运行，不把这项证据表述为全栈重启或业务恢复验收；旧工作树服务仍为 200 且未被停止或修改。

## 停止点与下一动作

当前活动切片是 R0 集成验收。下一步由所有者使用三份真实文字攻略验证：清晰单城、包含备选／说明／取消／改期的混杂文本，以及包含住宿、编辑和手动地图更新的多日文本。3/3 应返回卡片或诚实部分结果，至少 2/3 经少量编辑可用，至少 1/3 完成“卡片 → 编辑 → 路线需更新 → 手动更新 → Top-3 → PNG”。

收到体验结果后只修复真正阻碍使用的 P0/P1；R0 通过后再处理协同快照 CAS，并进入 R1 真实 Provider 与真实双用户验证。无需所有者检查日志或运行诊断命令。
