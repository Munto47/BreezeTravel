# 当前状态：第二轮界面已交付本地体验

目标 `TC-DUAL-MODE-HORIZONTAL-R0` 仍为 `IN_PROGRESS`。本轮落实所有者 2026-09-06 的两视图、地图首屏、自由拖拽、就地编辑与动画反馈，不声明整体 R0/R1/R2 或新版 G07 通过。

## 基线与保护

- 分支：`codex/soft-canvas-owner-feedback`。
- 工作树：`D:/CODEX/BreezeTravel-soft-canvas`。
- 本轮精确起点：`967414e`；此前 `91e92e4` 和第一次界面收口验证均为历史证据。
- 只保存可回退的本地 checkpoint；不推送、不发布、不合并 main。
- 原工作树、旧服务与用户已有 AGENTS.md 修改保持不动。

## 本轮用户结果

- 结果页只有“卡片／地图”；移除独立优先检查页面，不删除检查后端或改变安全状态。
- 地图占据剩余视口，不需要下滑才能看全。去掉大标题／状态面板和常驻住宿重试框；所有日期仍在同一张地图，日期颜色图例在地图下方。
- 路线方式、预演与住宿保留在地图下方的小浮层；地图失败时保留地点目录、诚实状态和重试，不画推测路线。
- 照片区域或把手均可拖动；同日、跨日与空日均可落点。显示完整插入占位、相邻卡片让位和微倾、跟手卡片，边缘自动滚动。
- 修复快速移动后立即松手使用旧落点的问题；松手按最终坐标判定，悬停不写入，应用一次命令；拖入删除区删除，Escape／触摸取消零写入。键盘等价操作、撤销和失败回退保留。
- 行程信息直接在原下拉框内编辑；保留未保存修改提醒，取消恢复原触发点焦点，不跳到远处。
- 生成动画增加漂浮纸片、轨迹、扫描光束、地点图标和完成印记；实际阶段仍只由服务端事件／权威进度推进。尊重减少动画设置，不伪造百分比或为动画强行延长生成。
- 编辑后旧路线立即失效；手动更新、PNG、POI 照片和纯文字入口保留。本轮不修改 API、数据库或后端产品实现。

## 本轮验证

`AUTOMATED_TEST`：

- 浏览器组合 91 passed、5 skipped：结果交互、渐进生成、协同入口边界。5 个跳过均标记 `RETIRED_CHECKS_PAGE`，仅对应所有者明确移除的独立检查页面，不算通过；原并发、隐私、自动路线与错误恢复断言保留。
- 四个目标视口地图首屏边界、下方图例、无滚动切日；真实鼠标跨日拖动和 Chromium 触摸跨日／取消已验证。不是 Safari 或实体手机证据。
- 后端文字主链、v3 API、POI、路线与地点身份安全：独立完整重跑 124 passed。首次与浏览器并发执行时 60ms 租约时序用例失败一次，随后单例与完整组合均通过；未改测试或后端代码，记录为时序敏感风险。
- 最终拖放／首屏定向复测 7/7 通过；TypeScript、Next.js 生产构建及本地服务重启通过。
- 一致性验证 PASS（CONTRACT_ONLY），不作为产品或真人证据。
- 对真实地图、动画和鼠标拖动预览进行了截图复核；地图截图等待侧栏收起与底图完成，避免过渡帧掩盖结果。
- 旧 40 条文字兼容结果仅属于上一检查点；本轮后端未变，但不把旧统计包装为本轮新执行。

`LIVE_PROVIDER_EVIDENCE`：

- 本轮生产构建完成真实文字→SSE→两日三卡→POI 照片→全日期地图的定向闭环，最终复测 1/1 通过。
- 一个实际事件连接，观察到“整理每天安排／收尾中”，三张照片加载成功；三标记均在视野内，地图上下边界均在首屏内。没有为展示动画强制延迟或编造进度。
- 只使用合成北京攻略与现有开发授权；截图和脱敏观察保留在忽略的 `frontend/test-results-live-soft` 与 `.local-artifacts/soft-canvas-live-report.json`，不提交凭据。
- 此结果不是三城、真实协同驾车、实体手机或整体 R1 的通过声明。

## 本地入口与边界

入口：**http://127.0.0.1:3118**。本地启动器管理 API 8018、Yjs 1246、PostgreSQL 55451、Redis 56401；只监听本机，不是公网部署。

三份所有者攻略的体验目标更新为至少一份完成“卡片→编辑→路线需更新→手动更新→PNG”，不再要求进入已移除的优先检查页面。整体真人验收仍未完成，不要求所有者检查日志或排查内部实现。

照片受实际 POI 供给影响，无图仍回退插画；快速生成可能略过短暂动画。Safari、实体手机、真实双用户协同／驾车、完整候选矩阵与整体真人验收：NOT_RUN。历史双模式后续 R1 工作不属于本次界面完成声明。

本轮产品代码、验证与文档以同一个本地 checkpoint 保存。下一自主动作：按所有者实际界面反馈修复阻碍使用的问题；不需要所有者排查技术问题。
