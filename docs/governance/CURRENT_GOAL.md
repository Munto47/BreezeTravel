# 当前状态：地图上移与候选同行确认

目标 `TC-DUAL-MODE-HORIZONTAL-R0` 仍为 `IN_PROGRESS`。本轮对应所有者第六轮反馈，已完成该界面切片；不声明整体 R0/R1/R2 或 G07 通过。

## 基线与保护

- 本轮起点 `0be5dfe`，分支 `codex/soft-canvas-owner-feedback`，工作树 `D:/CODEX/BreezeTravel-soft-canvas`。
- 仅本地修改、构建和检查点；不推送、不发布、不合并 main。原工作树、数据库、密钥与用户已有修改不动。
- 不改变 API、数据库、模型或 Provider 配置。历史证据不拼接为本轮通过。

## 用户结果

- 地图紧接行程标题区域，去除常规保存成功提示所占的空白。
- 地图状态、手动更新、撤销、保存与更多操作集中在标题旁的同一操作栏；地图内部不再重复放置更新按钮。手机紧凑换行，更多菜单不被裁切，路线按钮保留至少 48px 高触控区。
- 常规编辑、撤销、账号保存和路线请求确认不再显示解释性成功横幅；真实失败、并发冲突、未知保存结果和恢复入口保留。
- 候选首次点击只选中，右侧同行立即出现“确认”。再次点击同一已选候选同样确认；换选其他候选不保存。删除列表底部大确认按钮。
- 两种确认方式共用同步写锁，保存期间禁用选择／确认；快速重复点击只提交一次。
- 地点仍在卡片旁或地图内就近搜索与确认，不跳转、不增加遮罩。编辑后仅标记路线需要更新，零自动路线请求。日期颜色仍在地图下方。
- 先前的拖动、跨日、外侧回环、生成提示、图片、导出、住宿、撤销与隐私功能保留。

## 实际验证

`AUTOMATED_TEST`：

- 最终组合：**118 passed、5 skipped**。五项是此前停用的独立检查页测试，本轮无新增跳过。
- 新增 1440、1280、390、360px 四视口：确认按钮在选中候选右侧、无成功横幅、地图操作合栏、更多菜单可见、地图紧接标题并完整落在首屏。
- 新增六候选切换／再次点击／快速双击测试：改选零写、确认一次应用、等待中禁用、零自动路线请求。
- 地图状态迁到工具栏后，旧测试改为读取地图状态属性；写锁测试切到实际地图视图验证新按钮禁用，保留强制点击零请求、超时、幂等和恢复断言。
- 回归发现新操作栏路线按钮触控高度不足，已修至 48px 并重跑完整组合通过。未削弱原触控或安全检查。
- TypeScript、Next.js 生产构建、本地重启及 diff 检查通过。复核桌面／手机下拉框、紧凑地图及真实地图截图，不作为真人验收。
- 机器一致性检查 PASS（CONTRACT_ONLY），其 product_delivery 仍为 NOT_RUN，不代替产品验收。
- 后端未修改；后端完整测试、40 条语义集和旧完整候选矩阵未重跑。
- 测试环境仍有 Yjs 重复导入警告；本轮未涉及协同同步实现，不外推真实双用户结果。

`LIVE_PROVIDER_EVIDENCE`：

- 最终生产构建 **2/2 passed**：真实文字生成、事件流、POI 图片、全日期地图及六地点回环。
- 真实地图搜索“北海公园”后，候选右侧确认并保存，目录更新、地图视图保留并变为 NEEDS_UPDATE；一次命令、零路线写请求。
- 只使用合成攻略与现有开发授权，没有操作所有者存量行程。截图位于忽略的 `frontend/test-results-live-soft`。
- 不外推三城覆盖、协同驾车或真人易用性。

## 入口、剩余边界与下一步

**http://127.0.0.1:3118** 已重新构建当前源码；API 8018、Yjs 1246、PostgreSQL 55451、Redis 56401。数据库和草稿保留。

本轮要求已完成，保存本地 checkpoint 后交付当前页面。Safari、实体手机、真实双用户协同／驾车和所有者三份攻略验收仍为 NOT_RUN；它们不属于本轮界面改动的完成声明。

下一步为所有者体验当前页面；后续按实际反馈继续自主修复，无需所有者排查内部问题。
