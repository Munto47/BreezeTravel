# 当前状态：统一地点编辑与短导航

目标 `TC-DUAL-MODE-HORIZONTAL-R0` 仍为 `IN_PROGRESS`。本轮对应所有者第五轮界面反馈，不声明整体 R0/R1/R2 或 G07 通过。

## 基线与保护

- 本轮起点 `cdaec59`，分支 `codex/soft-canvas-owner-feedback`，工作树 `D:/CODEX/BreezeTravel-soft-canvas`。
- 仅本地修改、构建和检查点；不推送、不发布、不合并 main。原工作树、数据库、密钥与用户已有修改不动。
- 不改变 API、数据库、模型或 Provider 配置。历史候选和前轮测试不拼接为本轮通过。

## 用户结果

- 左侧导航高度收拢到两个按钮，不再从页面顶部延伸到底部；手机底部两项导航保留。
- 首页浅色提示改为“粘贴行程，帮你整理地点、核对路线，生成清晰的行程卡片。”，不额外铺设说明。
- 所有已有卡片（已确认／待确认）点击统一展开卡片旁的“地点”：键入关键词、显式搜索、选择候选、确认保存。移除旧详情弹窗与编辑文字、替换、移动位置、前后一天按钮。
- 地图选中地点后的修改同样在地图内按钮旁展开，不进入旧编辑工作区、不替换当前视图、不使用全局遮罩。
- 地点候选由现有服务返回，保持服务端顺序；本轮选择关键词搜索方案，不新增文字候选推断或自动确认。打开零搜索、选择零写入、确认一次写入，保留受权限约束的候选和并发／幂等／失败回读。
- 顺序通过拖动、跨日占位和卡片让位调整。键盘同样在原把手 Enter 提起，左右调顺序、上下换日期、Enter 放下、Escape 取消，不再弹出移动面板。
- 删除区拖拽、撤销、隐私和未知写入恢复保留。键盘 Delete 的防误删确认以及新增地点入口不在本轮已有地点编辑的移除范围内。
- 所有地点修改仍只使路线过期，不自动计算路线。外侧回环、距离与耗时、图片、生成提示、导出及住宿保留。

## 实际验证

`AUTOMATED_TEST`：

- 定向旧交互迁移与新行为验证：18/18 passed。
- 最终完整组合：**113 passed、5 skipped**；五项均为此前停用的独立检查页测试，无新增跳过。
- 新增四视口已确认卡片／地图就近搜索、无模态、无旧工作区、URL 不变、选择零写和确认单次写，桌面导航高度小于 160px；新增首页提示检查。
- 旧文字编辑的超时重放／路线失效测试迁移为候选确认，保留同一幂等键、单次应用、读写锁和零自动路线请求断言。键盘面板测试迁移为本地预览／跨日／取消，不削弱安全断言。
- 初次完整回归暴露旧占位提示和“编辑文字”测试入口已过时；同步到当前要求，隐私与无原文泄漏检查保留。没有新增跳过。
- TypeScript、Next.js 生产构建、本地重启和 diff 格式检查通过。桌面与手机卡片／地图下拉框截图已复核，未将自动截图当作真人验收。
- 机器一致性检查 PASS（CONTRACT_ONLY），不代替产品或真人验收。
- 后端没有修改；后端完整测试、40 条语义集、历史知识详情页和旧完整候选矩阵未重跑，不算本轮证据。

`LIVE_PROVIDER_EVIDENCE`：

- 当前生产构建 **2/2 passed**：真实文字生成、事件流、POI 图片、全日期地图和六地点回环。
- 在真实地图界面搜索“北海公园”，展示实际候选并确认后，目录更新为新地点，地图保持当前视图并标记 NEEDS_UPDATE；一次命令、零路线写请求。确认前选择候选无写入。
- 仅使用合成北京攻略与现有开发授权，无新服务或费用权限。截图位于忽略的 `frontend/test-results-live-soft`。
- 不外推协同驾车、三城覆盖或真人易用性。

## 入口与剩余边界

**http://127.0.0.1:3118** 已重新构建当前源码；API 8018、Yjs 1246、PostgreSQL 55451、Redis 56401，数据库和草稿保留。

Safari、实体手机、真实双用户协同／驾车、所有者三份攻略验收：NOT_RUN。本轮不添加自动推断候选的能力，用户用关键词搜索真实地点。无法查询时保留原地点并在原处重试。

本轮已完成并保存本地 checkpoint。下一步为所有者体验当前页面；后续按实际反馈继续自主修复，无需所有者排查内部问题。
