# 当前状态：首次服务器上传完成

## 目标与授权

本轮从 `2c8d9b0` 开始，在 `codex/soft-canvas-owner-feedback`、`D:/CODEX/BreezeTravel-soft-canvas` 完成真实验证、必要修复和既有服务器首版上传。所有者明确批准本次部署；未合并 main、推送代码、删除旧业务数据或改变同机其他应用。

本发布切片为 COMPLETED；整体 `TC-DUAL-MODE-HORIZONTAL-R0` 保留所有者体验待验收，不以首次上线声明 R1/R2、G07 或真人通过。

## 当前入口与用户结果

- 正式入口：**https://www.breezetravel.cn/**
- 本地入口：http://127.0.0.1:3118，已重建重启。
- 公网真实流程通过：匿名文字生成与事件流、地点搜索及同行确认、编辑后路线失效、手动地图更新、PNG、保存账号、分享访客访问、原文和行程删除。
- 公网双账号协同通过：创建／加入、地点同步、AI 地点、再次对话可恢复、显式排线、保存／重新打开、单向转入私有文字行程及另一客户端重新打开。
- 修复真实测试发现的分享路由未挂载、地图销毁后清理导致空白页、Windows 图检查点初始化、图检查点已知类型反序列化、排线响应模型不兼容和无日期保存后无法重开。
- 协同旧排线数据混有估算且缺乏逐段可核实来源，公开响应不再透传旧交通耗时，诚实显示路线暂不可用。行程查真实步行距离、耗时和地图已通过。
- 协同追问可能返回未找到或部分结果；测试证明恢复与后续操作可继续，不证明所有问答都能给出有用建议。

## 当前验证（不拼接旧版本通过结论）

- AUTOMATED_TEST：前端完整组合 **120 passed / 5 skipped**；五项为此前停用的独立检查页测试，无新增跳过。覆盖 1440、1280、390、360px。
- AUTOMATED_TEST：运行时、真实 PostgreSQL 隔离库、协同导入、分享、原文删除、留存等后端组合 **76 passed**。
- AUTOMATED_TEST：文字合流定向测试 **75 passed**；40 条兼容样例 **40 安全 / 37 精确 / 3 保守降级 / 0 危险输出**。
- LIVE_PROVIDER_EVIDENCE：服务器私有验证 **2/2 passed**；切换后的公网最终验证 **2/2 passed（45.8 秒）**，使用真实已授权模型／高德、合成文字和测试账号，无固定回放、无网络响应替换。
- 本地与服务器 Linux 生产构建通过；公网首页、API 健康返回 200，新 API／Web／Yjs 运行中。同机其他两个站点仍返回 200。
- 已目视复核公网地图和协同保存截图。协同截图中的受限提示不是路线成功。
- 机器一致性 PASS（CONTRACT_ONLY），product_delivery 仍 NOT_RUN，不代替产品门。
- HUMAN_USABILITY、正式 H1、Safari／实体手机、完整 G07 与实际回滚切换演练：**NOT_RUN**。

## 发布与恢复

运行目录 `/opt/breezetravel-releases/first-public-20260906`；444 个运行文件已逐项核对。源包 SHA256：

`766db19a926fec3f25b6fc467273c9ac1babc5164b890b03ddf292a568fccf89`

旧数据库、容器、镜像与 Yjs 数据保留。切流前暂停旧写入者，最终备份后复制到独立新库；旧数据库没有被新版覆盖。备份路径 `/opt/breezetravel-backups/first-release-20260906`。恢复边界见 `docs/operations/FIRST_RELEASE_20260906.md`。

凭据仅保存在权限隔离目录和服务器私有配置，未进入 Git／聊天／测试报告。原工作树用户 AGENTS 修改保持不动。

## 停止点与下一步

本轮上传与真实受控验证已完成，交付所有者使用正式网址。后续按真实使用的 P0/P1 反馈修复；不为正式候选自动扩大范围。协同驾车可信结果与追问质量仍是已知后续项，不能声称全项目所有功能均无问题。
