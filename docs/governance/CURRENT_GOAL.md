# 当前状态：界面反馈已落实，交付本地体验

目标 `TC-DUAL-MODE-HORIZONTAL-R0` 仍为 `IN_PROGRESS`，总体 R0 的所有者验收尚未完成。本轮仅收口所有者 2026-09-06 的界面与拖拽反馈，不把旧 G07、双模式或真人结论拼接为新版通过。

## 当前实现基线

- 分支：`codex/soft-canvas-owner-feedback`。
- 工作树：`D:/CODEX/BreezeTravel-soft-canvas`。
- 精确基线：`91e92e436b124affef4ce4d01defad5edd00bada`。
- 产品与状态以同一个可回退的本地 checkpoint 保存；不推送、不合并 main、不发布。
- 原始工作树、旧 3116 体验服务及用户未提交的 AGENTS.md 修改均未改动。当前工作目录是独立工作树，不在旧 checkout 继续混合开发。

## 本轮用户结果

- 首页去除标题、段落说明、前置条件与常驻保留期文案；圆润白色输入区仅用浅色占位提示，保留提交、简短示例、继续上次与隐私入口。
- 生成过程显示“收到攻略／整理每天安排／核对地点／收尾”，由真实事件和累计进度推进，结束进入可编辑卡片；不使用定时器伪造进度。快阶段可以直接变为已完成，不强制拖慢生成。停止、只读临时快照、断线续订和轮询降级保留。
- 结果页删去概览、教学说明及每张卡片底部的方向／交换／垃圾桶按钮；导出和列表切换收为紧凑工具栏。
- 横链两端高、中间低；悬停时留出卡片宽的插入空隙，相邻卡片移动。桌面与触摸均可跨日拖动，松开只应用一次移动命令；拖入公共删除区删除，取消零写入。键盘可通过同一把手进入移动／删除确认；撤销、冲突恢复和焦点恢复保留。
- 卡片改动立即隐藏旧路线耗时，仅用户手动更新路线。完整 PNG 同步采用下垂连线，仍可无地图、无照片导出。
- 地图默认展示全部日期、全部已确认地点与真实可用路段；颜色与日期一致，首次不再自动聚焦第一站。日期按钮只选择预演，地点目录与底部简卡保留全部日期。住宿折叠，地图／住宿失败仍提供简短重试。
- 新生成卡片从实际核实的 POI 同次搜索获取照片，不额外搜索地点，不改变城市、类别或身份阈值。无图／失败回退插画，待确认卡片不显示照片；改名、替换、改城市清除旧照片，纯移动保留。
- 可空照片字段同步当前 OpenAPI 和共享客户端；只允许可信图片域名与脱敏 HTTPS 地址，无凭据、查询参数或外部任意地址。40 条非 blind 兼容检查同步这个获批公开字段，同时增加恶意地址与未知字段拒绝测试；冻结数据、schema、blind、原始安全断言不变。
- 未恢复 OCR、图片输入或图片识别。

## 本轮实际验证

`AUTOMATED_TEST`：

- 最终当前源码一次性浏览器组合 **89/89**（结果页 72、渐进生成 3、双模式边界 14）；此前地图相关 17/17 定向重跑也通过。
- 双模式登录、房间、转换、隐私与手机边界 14/14；原测试的首页标题断言改为检查真实文字输入框，未删除登录返回行为断言。
- 桌面视口 1440、1280，手机视口 390、360 均检查；Chromium 真实触摸事件验证跨日移动与取消。不是实体手机或 Safari 证据。
- 后端地点、文字、v3 API 与路线组合 106 passed；照片命令补充断言后文字主链单独 57 passed。另一个兼容／体验／隐私／保留期组合 98 passed、17 skipped；跳过项需要独立 PostgreSQL 测试配置，本轮未重跑，不能算通过。
- 40 条文字兼容：40/40 safe、37/40 exact、危险输出 0；文字子门达到 R1，整体交付仍 NOT_RUN。三条保守降级为一条安全遗漏及两条该 runner 不执行的 UI 检查，未改为精确通过。
- TypeScript、共享客户端生成与构建通过；最新产品源码经本地启动器完成 Next.js 生产构建和服务重启。
- 主线一致性验证 PASS（CONTRACT_ONLY），不作为产品或真人通过证据。

`LIVE_PROVIDER_EVIDENCE`：

- 最新源码完成一次真实文字→SSE→两日三卡→三张 POI 照片→全日期地图的定向浏览器闭环。
- 观察到一个事件连接，以及“整理每天安排”和“收尾中”的实际活动状态；地点核对阶段已完成标记存在，未要求快速阶段必须停留固定秒数。
- 三张照片加载成功；真实高德底图和跨日三标记已逐个检查进入视野，并读图复核。初始截图过早且首站自动聚焦的问题已发现、修正和复测，不沿用仅组件存在的通过结论。
- 只使用合成的北京两日文字和现有开发授权，不用所有者原文。未据此声明真实协同／驾车、三城矩阵、整个 R1 或正式候选通过。
- 本地忽略目录保留 `.local-artifacts/soft-canvas-live-report.json` 与 `frontend/test-results-live-soft` 截图；不提交浏览器凭据或 Provider 密钥。

## 运行与体验入口

网页：**http://127.0.0.1:3118**。旧 3116 页面不会自动换成新版。

本轮隔离运行时使用 API 8018、Yjs 1246、PostgreSQL 55451、Redis 56401；由现有本地启动器管理。只监听本机，不是公网部署。

## 边界与下一动作

本轮界面请求可交付体验；总体 R0 仍需要所有者三份真实文字攻略：清晰单城、包含备选／取消／改期的混杂文本、含住宿与编辑的多日文本。至少一份完成“卡片→编辑→路线需更新→手动更新→Top-3→PNG”。此处不要求所有者检查日志或诊断内部实现。

照片受 POI 图片供给影响，旧行程不自动补搜图片；缺图仍可使用。生成很快时，中间阶段可能迅速完成，不为展示动画拖慢结果。Safari、实体手机、真实双用户协同／驾车、完整候选矩阵与真人验收尚未运行。

历史双模式的协同快照 CAS、真实驾车段公开投影与跨标签领取恢复仍是后续 R1 工作，不属于本轮界面完成声明。`HUMAN_USABILITY` 与整体 R0/R1/R2：NOT_RUN；公网、生产、发布与 main 合并：NOT_AUTHORIZED。

下一自主动作：按所有者体验反馈优先修复真正阻碍使用的问题；当前没有需要所有者排查的技术阻塞。
