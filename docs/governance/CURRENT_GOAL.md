# 当前状态：地点匹配优先优化完成（本地）

## 本轮目标与边界

从 `c6bea8f` 出发，在 `codex/soft-canvas-owner-feedback`、`D:/CODEX/BreezeTravel-soft-canvas` 完成最高可感知收益的地点匹配切片。先测真实服务，再修明确问题，不扩展通用解析器、并发平台或推荐功能。

**本次优化只在本地运行并验证，未更新公网。** https://www.breezetravel.cn/ 仍是首次上传版本；该次发布的恢复信息见 [首次部署记录](../operations/FIRST_RELEASE_20260906.md)。未合并 main、推送、删除旧业务数据或更改线上配置。

## 用户结果

- 鸟巢、水立方、东方明珠等常见简称经受审名称提示，重新查询真实主地点；后海和武康路可以按精确名称及技术类别确认。
- 只补五个有来源的名称／类别提示，不保存固定 POI ID、坐标、耗时或价格，不将旧词典或模型猜测当作真实地点。
- 主地点必须满足唯一性、城市、行政区、精确名称和相容类别；子地点不能借用父地点别名自动通过。
- 手动搜索按主地点精确匹配优先排序，再截取六项；体育场主地点不再被过滤掉而仅留下走廊或活动。泛词搜索继续保留。
- 修复原技术类别兼容规则可能把含酒店标签的场馆当景点的问题；已知类型与完整标签共同校验，不放宽类别底线。
- 上海博物馆未指定馆区时继续待确认，用户在原下拉框选择人民广场馆或东馆。
- 编辑仍一次写入、零自动路线请求；地图只由手动更新。现有四并发与去重已具备，不为优化额外加并发或模型调用。

## 实际测量

同一组 18 个北京／上海／杭州常见地点，使用既有授权的真实高德：

| 观察 | 优化前 | 优化后 |
|---|---:|---:|
| 自动匹配 | 12/18 | 17/18 |
| 高德请求数 | 25 | 20 |
| 五个修复地点的累计查询耗时 | 2389ms | 971ms |
| 18 点累计串行测量耗时 | 6590ms | 5132ms |

这是小型定向样本，不是全城覆盖率、盲测精度或生产 P95。五个地点从两次请求缩为一次是确定的请求减少；耗时受网络影响，不能外推整段生成快 59%。一次中间复测及最终复测均为 17/18、20 请求；最终 0 意外匹配、0 服务异常。

受审名称来源与观察详见 [地点优化记录](PLACE_MATCHING_ROI_20260906.md)。原始 Provider 响应没有留存。

## 验证

- AUTOMATED_TEST：地点、候选、文字兼容、v3/API 等组合 **245 passed**，其中新增匹配边界 **23 项**；Ruff 和 diff 检查通过。
- 40 条既有文字样例重测：**37 精确 / 3 安全降级 / 40 安全 / 0 危险**。不把本次地点样本当作该语义指标提升。
- LIVE_PROVIDER_EVIDENCE：最终本地生产前端＋真实 API／Qwen／高德浏览器 **2/2 passed（20.9 秒）**。北京文字生成 6 张正确已确认卡片；鸟巢搜索主地点第一、再次点击确认一次、无模态/自动算路、手动地图更新成功；上海 3 张确认、博物馆待确认且可选两个馆区。
- 已目视复核真实卡片截图；前端生产构建通过。本地 `http://127.0.0.1:3118` 使用最终后端，API/Web/Yjs/PostgreSQL/Redis 状态正常。
- MULTI_AGENT_SIMULATED_REVIEW：只读复核定位并复现父子 POI 和技术标签问题，主任务修复；不是外部或真人验收。
- 额外扩测发现旧固定解析器长攻略 **2 failed / 7 passed**；内存回读 HEAD 原版模块后相同两失败。该链不使用本轮 AMap/候选模块，相关解析器无改动：张家界重复可选提及、秦岭摘要／正文／海拔表重复出卡。保留失败，不改测试期望，不算本轮通过。
- 未重跑全部前端/协同组合、全量后端、实体手机、Safari、正式真人／候选矩阵；本轮未部署，不能声明公网优化效果。

## 后续

1. 交付当前本地优化；下一次公网更新须取得明确授权，并沿用独立发布／备份／真实回归流程，不能重跑首版切换脚本。
2. 继续以真实用户文本中的无法匹配或误匹配为优先；地区／馆区不明确的内容保持可手动纠错。
3. 后续再考虑一项餐饮建议和一项住宿建议：复用现有地点和住宿能力、按需展开、不自动改行程、不虚构价格或房态。当前住宿功能保留，本轮未新增推荐功能。
4. 旧固定解析器长攻略去重单独排期，不用全局名称去重破坏真实再次到访；协同驾车受限和追问质量仍保留此前边界。

本轮切片 COMPLETED；不声明整体 R1/R2/G07 或真人通过。
