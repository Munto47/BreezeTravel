# 当前状态：紧凑回环卡片已交付本地体验

目标 `TC-DUAL-MODE-HORIZONTAL-R0` 仍为 `IN_PROGRESS`。本轮只交付所有者 2026-09-06 新要求的回环卡片与相对顺序；不声明整体 R0/R1/R2 或新版 G07 通过。

## 基线与保护

- 分支：`codex/soft-canvas-owner-feedback`。
- 工作树：`D:/CODEX/BreezeTravel-soft-canvas`。
- 本轮精确起点：`a6ec335`。此前重力链、两视图验证为历史，不拼接本轮结果。
- 只保存可回退的本地 checkpoint；不推送、不发布、不合并 main。
- 原工作树、数据库、密钥与用户已有 AGENTS.md 修改保持不动。

## 本轮用户结果

- 一天的卡片改为紧凑回环：第一行从左向右，下一行从右向左，自动继续折行。容器决定 1–6 列，桌面目标 4–6 列、360/390px 手机两列，卡片最大宽 184px。无整日横向滚动。
- 卡片顶部相接的曲线与行末转弯表达先后顺序，序号与 DOM 保持逻辑顺序。真实步行／公交及分钟数仍只消费现有地图结果；未知与过期如实标记，顺序曲线不是推测地理路线。
- 卡片、临时快照、地点详情、编辑器及 PNG 不显示／要求到访时刻或停留时长。既有时间字段与数据保留，不改 API、数据库或后端核验。
- 保留图片／把手拖动、跨日与空日、键盘等价操作、插入预览、让位、删除区、撤销和错误回退。修正反向行位置换算；拖动卡片只占一个位置，源日期高度保持稳定，避免下一天突然下移导致落错日期。
- 修正响应式重排时卡片和连接线不同步；普通排版立即定位，拖动让位才使用弹性位移动画。关闭编辑后在画布 DOM 就绪再恢复焦点。
- PNG 复用同一回环坐标逻辑，包含完整日期和屏幕外卡片，保留待确认／过期／受限状态。不依赖照片或地图底图，ETag 改变仍终止导出。
- 卡片／地图两个视图、地图首屏与下方日期图例、生成动效、照片、住宿、隐私和手动路线更新保持。

## 实际验证

`AUTOMATED_TEST`：

- 最终完整浏览器组合：**97 passed、5 skipped**。跳过的 5 条均是上轮已标记的 `RETIRED_CHECKS_PAGE`；本轮没有新增跳过。覆盖结果交互、SSE 停止／续订／降级和协同入口边界。
- 新增 13 卡长日四视口验证：1440、1280、390、360px；全卡片不越出画布、无横向溢出、反向行顺序、卡片与弧线锚点一致。
- 新增反向行真实鼠标落点与一次写入；保留 Chromium 触摸跨日／取消、跨日挤压、空日移动、删除／撤销、未知写入幂等恢复、隐私和零自动路线调用。
- 新增完整 PNG 验证：13 卡含屏幕外第三行、第二行反向、待确认与 12 个过期路段标记，不显示绝对时间；真实下载截图已复核。
- 旧时间编辑的两条恢复／路线失效测试改用文字编辑，保留原安全断言；旧重力曲线和宽占位断言改为当前用户要求，未降低安全门槛。
- 初次回归暴露了触摸时源卡与占位重复挤出一行、响应式动画造成卡片与曲线暂时错位、编辑关闭焦点恢复时序问题，已修正并完整复跑。PNG 新测试首次因只设置详细地图而没有同步结果地图的 fixture 状态失败，已修正测试输入，断言未减。
- 最后移除旧时间列留下的窄列占位后，编辑／恢复／路线失效／四视口回环／PNG 定向补测 **9/9 passed**；再次生产构建和本地重启通过。
- TypeScript、Next.js 生产构建、本地服务重启通过。机器一致性检查 PASS（CONTRACT_ONLY），不作为真人或整体交付门证据。
- 本轮未修改后端；后端完整测试、40 条语义样例和旧候选统计未重跑，不计为本轮新证据。

`LIVE_PROVIDER_EVIDENCE`：

- 当前生产构建已跑真实文字生成、实际事件流、POI 照片、全日期地图与首屏边界的闭环。
- 额外使用六地点合成北京文字攻略，验证桌面折行与手机两列，不含到访时间；照片可加载，未引入照片服务或新数据权限。
- 只使用合成攻略与现有开发授权。截图和脱敏观察在忽略的 `frontend/test-results-live-soft` 与 `.local-artifacts/soft-canvas-live-report.json`，不提交凭据。
- 最终实时定向复测 **2/2 passed**：六地点长日已有真实步行时间标签（首段 38 分钟），桌面 5 列、手机 2 列；另一路覆盖实际事件流与全日期地图。新增可见性断言首次误查零尺寸布局容器，改查实际交通标签后通过；不是隐藏失败或伪造 Provider 成功。不能外推三城、协同驾车或整体 R1。

## 本地入口与未运行边界

入口：**http://127.0.0.1:3118**。API 8018、Yjs 1246、PostgreSQL 55451、Redis 56401，数据库和匿名草稿保留。当前源码已重新构建，不是旧页面。

Safari、实体手机、真实双用户协同／驾车、完整候选矩阵和所有者三份真实攻略验收：NOT_RUN。照片由 POI 实际供给决定，缺图仍使用插画。长日会增加页面纵向长度，但不会漏掉行末或下一行卡片。

本轮产品、测试、文档保存在同一个本地检查点。下一自主动作：根据所有者实际体验修复阻碍使用的问题；无需所有者排查内部技术问题。
