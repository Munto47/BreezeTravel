# P0 当前仓库基线（2026-08-22）

> 目标：记录「行程查」开发开始前的代码、migration、测试与 evidence 事实。本文不是 V1 放行报告。

## 1. Git 绑定

- Phase 0 起点：`85e1670ce0d85656be09311285bb9472af28517a`；
- 复验代码/权威候选：`3d905d3d72dba23027be821a4ad22035b390656b`；
- 分支：`codex/trip-check-p0-guidance`；
- 远端：`origin/codex/trip-check-p0-guidance`；
- 复验开始时工作树：clean；
- tracked files：883。

本报告提交只新增/更新治理文档，不改变上述已测试的 Python/TypeScript 运行代码。

## 2. 数据库基线

最新 migration 是 `021_atomic_suggestion_undo.sql`。最近五个 migration：

```text
017_changed_route_edge_refresh.sql
018_import_place_receipts.sql
019_suggestion_sets.sql
020_recommendation_event_commands.sql
021_atomic_suggestion_undo.sql
```

V1 新 migration 按规格从 022 开始；本阶段没有创建、修改或执行 migration。

## 3. 本次实际执行

| 层级 | 命令 | 结果 | 能证明什么 |
|---|---|---|---|
| 后端离线 | `cd backend; python -m pytest tests -q` | `1227 passed, 25 skipped, 38 warnings`，47.49s | 当前离线 suite 在候选代码上通过；skipped 层级未被证明 |
| Python 静态检查 | `cd backend; python -m ruff check app evals scripts tests` | PASS | 当前 Python 范围无 Ruff 错误 |
| 前端构建 | `cd frontend; npm run build` | PASS，Next.js 15.0.5，10 个路由完成 build/type check | 当前前端可生产构建；不等于浏览器业务链通过 |
| 旧数据集结构 | `python backend/scripts/validate_dual_entry_testset.py` | `structurally_valid=true`，`release_ready=false` | 旧数据文件结构可读，不是 V1 360 条 Gate |
| manifest 定向测试 | `cd backend; python -m pytest tests/test_dual_entry_release_manifest.py -q` | `2 passed` | V1 权威 hash 可绑定，manifest 保持 `REJECT/BASELINE_ONLY` |
| manifest Ruff | `python -m ruff check ...build_release_manifest...` | PASS | Phase 0 manifest 兼容修正通过静态检查 |

后端警告包括 sklearn 在 Windows/MKL 小数据块下的已知 KMeans 提示；它属于冻结 Planner 资产，不在 P0 扩修范围。

## 4. 旧数据与 evidence 边界

旧 `dual_entry_v1` validator 报告：

- 96 条：北京/上海/杭州各 32；
- Import 55、Builder 41；
- pilot 6 / dev 60 / regression 12 / frozen blind 18；
- human ground truth case count：0；
- `release_ready=false`；
- G2 四站种子 3/9，G5 恢复种子 6/9；
- weekly live、外部 blind bundle、真人校准等仍为 blocker。

`backend/evidence/` 中存在旧 local_eval、runs、real_provider_local_authorized、three_city_rc1、releases 等目录。这些产物没有在本阶段按 V1 同一 commit/config/dataset/model/receipt 重跑，只能作为历史资产索引。

## 5. 本阶段明确 NOT_RUN

- G2：V1 PostgreSQL migration/事务/并发/恢复；
- G3：V1 固定 Provider snapshot 与重放 hash；
- G4：真实高德、和风天气、Brave；
- G5：V1 浏览器主链、断线/重启、隐私清理与性能；
- G6：V1 release candidate manifest；
- 独立无 API `automated_proxy_judge`；
- 真人内测、公网 E2E、部署与发布。

因此当前正确结论是：P0 指导与基线完成，V1 产品实现尚未开始，整体 release decision 必须为 `REJECT`。
