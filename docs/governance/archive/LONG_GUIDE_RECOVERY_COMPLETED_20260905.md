# TC-LONG-GUIDE-RECOVERY 完成记录

完成日期：2026-09-05。范围是长篇 Markdown 攻略的格式锚点、并列地点保留、地点身份安全和失败恢复；不包含双模式、横链画布、OCR、部署或候选发布。

最终用户可见结果：真实三日攻略返回可编辑的部分结果，保留主要到访地点和两个互斥备选；未能唯一核验的地点显示待确认。编辑、刷新、撤销和用户主动更新路线均可完成，编辑过程没有自动调用路线 Provider。

自动证据：产品后端 246 通过；原文锚点与地点身份定向检查 41 通过；前端失败恢复浏览器场景 15 通过；生产构建和静态类型检查通过。真实 Provider 证据保存在未跟踪的 `after6-*` 记录中，结果明确为部分可用和地图受限，不推断为完整候选通过。

已知边界：菜名可能被保留成待确认卡片；地点召回仍可继续改善。两者均没有被自动确认成错误地点，不阻塞本修复目标完成。
