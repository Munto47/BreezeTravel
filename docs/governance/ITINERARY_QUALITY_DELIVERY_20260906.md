# 第一版行程质量强化交付记录

基线：2026-09-06 第一版 `a6035de874948029aa593104626c1ac40081d986`。工作树 `D:/CODEX/BreezeTravel-enhancement`，分支 `codex/itinerary-quality-20260906`。按新版 AGENTS.md 和所有者本次授权实施；原工作区、第一版工作树、线上版本保持不动。本记录只描述本轮本地能力，不继承旧候选、真人或生产结论。

## 已交付能力

| 用户操作或检查 | 现在的行为 | 可靠性边界 |
|---|---|---|
| 粘贴不同格式行程 | 逐日卡片保留明确空日、乱序日期、取消／改期后的安排；处理短句、箭头、表格和长文 | 不承诺任意文本都能完全理解；无法确认的内容允许保留待确认 |
| 查看时间 | 明确钟点、参观时长才写入结构化时间；否定、更正、模糊时段、交通耗时分别处理 | 不把“上午”补成9点，不把车程当游览时长，不把准备预约当已锁定 |
| 跨城行程 | 每个活动按原文城市依据核对地点；无法确定归属时不在多城盲选 | 北京、上海、杭州有深入地点／路线支持；其他国内城市为基础整理 |
| 查看备选 | 原文备选收在当日建议中，点击“加入待确认”才加入卡片 | 备选不自动搜索地点或加入地图；没有依据的备选城市可空 |
| 时间和交通检查 | 明确重叠、路线不足、过长路段、时长矛盾和顺延条件分别判断 | 缺失、未来、过期、冲突路线事实不能当作成功；交通不足时不自动顺延 |
| 重复安排与餐休 | 提醒当天重复到访、早餐／咖啡不能覆盖全天用餐；可预览加入用餐休息 | 不自动删除真实再次到访，不编造12:30或45分钟 |
| 找附近餐饮 | 在用户选中的已确认地点附近查询，同城真实餐饮最多3家，一次点击加入 | 半径1200米；仅按位置关联，不声称口味、质量、营业、价格或可订位 |
| 选择住宿 | 复用过夜首末站和现有连锁酒店检索；按有效路线比较，展示最多3家 | 未知通勤显示未知；内部缺路评分不再变成“120分钟”；无价格、房态或星级保证 |
| 跨城住宿 | 明确提示按过夜城市分别选择，阻止历史单酒店候选误套全程 | 本轮尚不自动生成跨城逐晚酒店组合；可自行添加并确认住宿卡片 |
| 建议采纳和撤销 | 候选绑定当前行程；一次写入、旧候选拒绝、可撤销，地图提示需更新 | 编辑和住宿选择都不自动算路线，必须明确点击更新地图 |
| 理解检查结果 | 在现有结果页展开“检查与建议”，最多三项优先处理，显示缺失信息范围 | 营业、预约与天气没有适用实时来源，明确未核验；未知不是通过 |

## 实际验证

### 后端、数据库和浏览器

相关后端 **509 项不同测试通过、无跳过**，分组串行执行，避免本机内存压力。住宿时钟／餐饮／检查／地图组合为110项，其中2项与199项组重叠；最后查询修复新增18项，并重跑82项住宿及数据库相关测试，计数已去重。

- 110：餐饮候选与命令、查验质量、实际数据库行程链、时间候选、顺延、地图住宿、优先建议和两个数据库闭环。
- 199：v3／API、文本收敛、真实地点适配器边界、地点身份安全、v3／地图住宿／检查数据库。
- 83：原文入口、删除、保留期、回读、运行时和交付合同。
- 101：当前语义适配器、文字保真、原文定位、十二类任务。
- 最后18个新增查询／品牌／类别／坐标边界包含在82项住宿最终复测中。

新增建议界面 **12/12 浏览器测试通过**：默认收起不查询、明确选择餐饮锚点、一次采纳、旧候选不可用、失败重试、迟到结果丢弃、备选待确认，以及1440／1280／390／360像素布局和Escape焦点恢复。已目视复核桌面和手机截图。

三条真实服务旅程分别通过，固定响应测试与真实外部服务结果分开计数：

- 北京餐饮：4张正确确认卡片、1个备选；实际查询3家餐饮，加入／回读／撤销成功，只有手动更新触发1次地图请求，真实地图可用并导出PNG。最后复测10.9秒。
- 沪杭手机：3天保持2／0／2张卡片，4个地点城市和实际坐标正确，第2天空日及武康路备选保留，没有自动查询备选或请求地图更新。
- 北京住宿：最终11.5秒通过；真实返回3家连锁酒店，已测最长单程为29／26／30分钟；选择首家恰好一次，过夜日锚点序位、结果／住宿回读及刷新保持已选均通过，0次自动地图请求。已目视复核三家候选和已选截图。

所有测试只创建原创合成行程；每份均按本次所有权单独删除，并回读确认410，没有删除已有业务数据。安全汇总见 [真实旅程记录](itinerary_quality_journeys_20260906.json)。住宿查询追加修复只重跑受影响的住宿旅程，没有重复调用已通过的餐饮和沪杭流程。

前端生产构建、共享客户端类型检查、OpenAPI一致性、修改的Python文件Ruff、git diff检查通过。核心一致性脚本只表示文档／合同一致，不表示产品或真实服务通过。

### 同数据真实模型测量

24份原创合成文本，共12类：普通多日、短句箭头、Markdown表格、无日期、乱序日期、跨城、取消恢复改期、并列备选、摘要重复、再次到访、无名餐休、长文分组。样本和预设期望未在修复中改变，不使用用户攻略或历史盲测数据。

| 实际运行 | 完全符合 | 调用／修复 | 输入／输出token | 累计调用耗时 |
|---|---:|---:|---:|---:|
| 第一版基线 | 24/24 | 24／0 | 42475／8026 | 60.53秒 |
| 中间版本首次 | 23/24 | 25／1 | 56536／9806 | 79.11秒 |
| 中间版本复测 | 22/24 | 25／1 | 不完整 | 102.52秒 |
| 最终交付代码 | **24/24** | **24／0** | **53875／9558** | **73.43秒** |

完全符合比较执行地点、日期、顺序、备选、公开卡片槽位与预设明确时间。使用真实语义服务，但地点解析器故意拒绝匹配，因此此表**不是地点准确率、全量文本成功率、盲测或生产延迟**。两版最终均24/24，不声称该指标提升；新增校验与实际用户流程另有定向测试和真实旅程证据。新版输入长度和耗时有所增加，不声称更快或更便宜。当前配置未提供计费单价，费用记录为未知，不当作零费用。

中间失败保留：更正后的文本被词法完整性规则要求重复包含已取消地点；另一轮长文触及既有30秒调用期限。修复采用语义与原文验证处理更正叙事，词法补漏仅针对不含更正的明确列表；没有篡改期望或增加无限重试。原始中间报告仍在忽略目录，四轮安全汇总已版本化： [测量汇总](itinerary_quality_measurements_20260906.json)。测量期间运行时代码指纹保持不变；随后只追加独立住宿查询修复，语义适配器、卡片投影、模型配置与样本均未变化，不重复调用模型。

### 失败恢复与未运行项目

首次建议面板有桌面出屏问题，修复后四视口通过。实际数据库发现重复刷新时间重叠检查的标识碰撞，修复后重复查验通过。真实住宿验收发现将请求开始时刻当响应校验时刻、Windows两类时钟精度不同导致拒绝有效响应；修复后生产使用响应后的墙钟，注入测试日期使用独立单调计时，未来／过期反例仍拒绝。

住宿复测又发现首版检索重复返回最近25家普通住宿，2／4／8公里和同城四轮都没有注册连锁品牌。保留同一类型／半径／城市限制，将单个查询关键词限定为“连锁酒店”，同一合成中心点在2公里一次请求得到4家符合已有品牌规则的酒店；再进行路线评分，而不由查询词直接认定品牌或服务质量。没有增加分页、搜索轮数或价格数据。使用单关键词符合[高德现行POI 2.0文档](https://lbs.amap.com/api/webservice/guide/api-advanced/newpoisearch)；旧版多关键词接口描述未沿用。

一次大批量后端执行因本机内存紧张触发线程启动超时，改为较小串行组后上述509项通过，未改变断言。前端没有配置ESLint，`next lint`会进入交互配置，本轮未添加依赖或配置，**不声明前端lint通过**。

未运行全仓库测试、完整协同回归、Safari、实体手机、真人体验或生产验证。本次没有修改协同运行时。基线固定解析器另有2个长攻略旧失败，另一个旧地点解析测试无名活动期望与现有行为不符，已在基线复现；这些冻结旧路径没有被计入509项或伪称通过。当前真实语义主链的长文已纳入本轮测试，但不据此宣布旧解析器问题已修复。

## 体验、维护和回退

本地入口：`http://127.0.0.1:3137`。API为8037，数据库为55517，缓存为56417，协同连接为1267；使用隔离本地数据和私有配置，与第一版3118分开。截图、PNG和脱敏详细报告在本工作树 `.local-artifacts`，密钥与原始响应不进入提交。

在本工作树执行现有管理脚本即可继续运行：

```powershell
Set-Location D:/CODEX/BreezeTravel-enhancement
& D:/CODEX/BreezeTravel/.venv/Scripts/python.exe scripts/experience.py status
# 如服务未运行：
& D:/CODEX/BreezeTravel/.venv/Scripts/python.exe scripts/experience.py start
```

修改代码后使用同脚本 `restart`；`stop`停止这份隔离服务且保留数据。不要运行首版公网切换脚本。本轮没有新增数据库迁移或生产依赖，未推送、合并main或部署。

可直接粘贴以下合成体验文本，展开“检查与建议”，选择景山附近餐饮，加入后撤销，再手动更新地图。住宿选择在同一面板中；未知或暂不可用应显示明确结果。

```text
北京两天。第1天：09:00故宫博物院，参观2小时；然后景山公园。北海公园只作备选。
第2天：天坛公园，然后前门大街。
```

跨城样例可使用 [24份合成样本](../../backend/eval_data/itinerary_quality_v1/cases.json)。源码回退可继续使用原来的 `D:/CODEX/BreezeTravel-soft-canvas` 第一版工作树，或从基线另建工作树；不需要重置、删除本次数据或覆盖用户修改。最终本地checkpoint由提交记录标识。
