# 七份实际攻略复现记录

本轮以本地已交付的 `5472927` 为基线，在原隔离工作树 `D:/CODEX/BreezeTravel-enhancement` 继续处理所有者报告的提交错误、地点遗漏、角色及顺序差异。只使用本次明确授权的七份原文复现；原文和逐次截图留在忽略目录 `.local-artifacts/owner-seven-20260906/`，不提交为训练或长期评测数据。新增可提交回归均为原创合成文本。

## 实际发现与修复

- 首次现场 API/Web 仍存活，但数据库和缓存没有监听，健康检查返回503。已恢复既有本地服务并保留数据；进程退出触发原因没有证据确定。服务状态现在同时检查依赖与健康响应，不能只凭端口显示可用。
- 首页分别处理服务暂不可用和提交结果未知；保留文字及同一请求身份，重试不新建另一份请求。不使用假成功或自动无限重试。
- 长文中并列地点、括号票价与馆区/楼层混在一起，曾被误当不可拆分列表或无来源名称。仅允许有唯一原文对应的封闭格式整理，保留完整原始位置、分馆、楼层和子地点；英文品牌不允许截断前缀，不合并不同英文单词。
- 明确备选和整日A/B方案保留在原日，不能默认选A。主线里的简短散步、逛一圈、落日站不因描述简短而消失；内部眺望对象、索道上下、票价和菜名不成为自动搜索的地点。
- 跨天同名地点必须引用本日真实出现的位置。只在唯一、顺序明确的Day正文中校正出现次数；更正、迁移或重复歧义退出这项自动校正。
- “前门”曾被自动确认成“前门博物馆”。已封住无类型简称通过删除场馆后缀确认身份的入口；精确名称与已核对别名仍需真实地点服务校验。
- 吃饭的街道保持街道类别。每日提供紧凑备选入口，整天未选择的方案不会只显示一行零地点而隐藏选择。

## 验证过程

保留每次完整结果，不能从不同版本各挑一次成功拼成最终通过：

| 运行 | 页面生成结果 | 尚存在的差异 |
| --- | --- | --- |
| before | 4/7返回结果 | 第3、6、7份失败；已返回结果也有城市查询与角色问题 |
| after-r1 | 5/7返回结果 | 第5、6份失败；发现前门错误确认、整日备选变主线等 |
| after-r2 | 7/7返回结果 | 仍有跨天同名绑定、武康路遗漏、田子坊角色及分支落日站遗漏；不算语义验收通过 |
| after-r3 | 5/7返回结果 | 第2、7份失败；上海备选仍漏教堂和商圈，且同名午餐备选未显示 |
| after-r4 | 6/7返回结果 | 第2份失败；第6份多出区域说明和重复用餐区域，第7份漏整日替代 |
| after-r5 | 6/7返回结果 | 第7份失败；第2份漏未加粗的标题二选一，区域收口因别日附注未触发 |
| after-r6 | 7/7返回结果 | 主线及备选多数符合；第6份两项午餐街道被分开输出为必去，仍不算语义全部通过 |
| after-r7 | 5/7返回结果 | 第6份楼层限定遗漏，第7份无名入住被补酒店名，两份被原文校验拒绝 |
| after-r8 | 6/7返回结果 | 第6份仍漏明确备选，拒绝假成功；独立反例发现短引用吞酒店名、跨行只选失效和后续同名备选被覆盖，随后均修复 |
| after-r9 | 6/7返回结果 | 前6份71个有名主站/31个备选对齐预期；第7份仍因重复备选位置与时间依据失败；第5份未确认街区类别需收口 |
| after-r10 | 6/7返回结果 | 同一模型温度0；第6、7份完整返回，第4份一个原文引用两次无法定位；不能把不同轮成功拼接为全部通过 |
| after-r11 | 7/7返回结果 | 第4份午餐的前门大街被无名用餐替代，仍不算语义通过；本轮另有两个源引用守卫在API启动后改动，不能认作完整最终版本证据 |
| after-r12 | 4/7返回结果 | 第1、2、7份失败；并列项、原文引用和重复分支仍未稳定，保留全部失败 |
| after-r13 | 6/7返回结果 | 前6份71个有名主站、31个备选逐项符合，46已确认、25待确认；第7份两次日期引用不匹配，仍未通过 |
| after-r14 | 6/7返回结果 | 第7份保留全部备选返回，第5份两项日期引用两次不匹配；不能视为稳定通过 |
| after-r15 | 6/7返回结果 | 前6份71个有名主站/31备选符合；第7份修正后仍缺明确可选咖啡，仍未通过 |
| after-r16 | 6/7返回结果 | 第7份仍有时间引用问题，并漏整日迪士尼与B方案北外滩；未拼接其他轮次成功 |
| after-r17 | 7/7返回结果 | 82个有名主站、46个备选逐项符合；49已确认、33有名待确认。启动衔接与之后两处文本相交保护变更使本轮不作为最终版本交付证据 |
| after-r18 | 7/7返回结果 | 82个主名、47个备选；第2份多出奥林匹克公园备选且顺序不符，仍未通过全部语义检查 |
| after-r19 | 7/7返回结果 | 第7份两家附近午餐推荐被排成两站必去，其中一家自动确认了具体餐厅；84主名、46备选，不算通过 |
| after-r20 | 6/7返回结果 | 第7份第一稿仅无据时间，第二次为修时间破坏一项原文引用，整份失败；保留失败 |
| after-r21 | 7/7返回结果 | 82主名、46备选的日归属/数量/顺序逐项符合，48已确认、34有名待确认；本轮未实际触发首稿恢复。随后仅收口餐饮名单肯定/否定订位边界 |
| after-r22 | 7/7返回结果 | 当时代码82主名、46备选逐项符合；47已确认、35有名待确认。第2、3份有2项/1项时间降级，仅地点语义通过；未实际触发首稿恢复 |
| after-r23 | 6/7返回结果 | 与r22同一代码，第2份修正后仍缺加粗并列组；其余6份返回，不以r22成功覆盖本轮失败 |

最终同版本复测和交付状态待本轮实际检查完成后填写。中间直接模型诊断只用于缩小错误，不能代替页面验收或真实地点证据。

中间合成集保持同一24份原创文本及预设答案：`owner-seven-noun-regression-final.json` 实为23/24（别名注释错误），保留失败；随后 `owner-seven-noun-regression-r2.json`～`r5.json` 各24/24。`r6.json` 为23/24（未知目的地被模型写为null导致结构失败），安全恢复为“目的地待确认”后`r7.json`为24/24、24调用、0修复。命名不代表通过，后续以最终运行时代码对应的实际结果为准。

温度0比较后的`owner-seven-noun-regression-r8.json`为24/24、24调用、0修复；没有新增模型或服务，运行时记录温度并维持两次共享60秒。温度0不能保证服务逐字确定，仍须原文与身份校验。对应895项后端解析边界和141项数据库/API回归通过，12项桌面/手机浏览器回归通过；前端生产构建、客户端类型检查、Ruff与diff通过。核心校验只有CONTRACT_ONLY PASS，产品验证字段为NOT_RUN，不能替代真实页面。

后续原创集r9因测量过程中运行时代码变化而中止，不能拼接为通过；r10为24/24、24调用0修复，r11为24/24、25调用1修复。所有页面提交一直使用首页的trim，早期直接模型探针没有去掉文件尾部换行；探针自exact-ui-source起与页面一致。旧页面记录的source_sha256是文件hash，不证明实际提交带换行；之后另记submitted_source_sha256。实际API读取的模型、prompt/schema与本地配置已独立对齐。

同一模型在相同总输出上限下启用512思考预算的私有实验：第1、7份地点语义符合，第2份仍失败，因此未采用，运行时保持非思考。随后缩小复现确认：无名描述里的标点被误判为地点列表；日引用检查提前退出导致其余错误未进入唯一修正机会；列表展开后的错误下标对应的却是展开前JSON。已分别修复，并让修正请求只携带相关错误和逐字原文候选，原文数据不进入日志。

独立合成反例另复现“早点出发／步行半天”被当作地点名并发起两次模拟地点查询。现仅拦截完整动作短语，保留包含同字词的完整店名；18项专门反例及相关267项验证通过。第7份尚反复遗漏B方案最后的简称滨江站，正在以严格未选择分支与逐字到访依据收口。

跨分支简称保护通过28项独立合成测试，包括已选/不执行、提前改期、引用、已有同名及错序。after-r15对应1068项去重后的解析/身份边界、141项数据库/API、12项浏览器检查通过；原创集r12和r13各24/24、24调用0修复。第7份剩余明确可选标签补齐仍须按同版本全部原文复测，尚不以测试数量宣称交付。旧列表修正测试的模拟初稿改为宽引用以继续覆盖实际模型修正；保留原有四站和两次调用断言，并检查逐字帮助只进入同次请求、不进入日志，未降低名称数量或安全标准。

原文明确可选标签补齐后，35项独立反例保护已选、取消、改期、引用和任意片段相交。局部JSON更新的私有模型试验仍失败，未进入运行时；但诊断显示无执行含义的远眺对象REFERENCE会阻止整组备选补漏。现在只整理OPTIONAL顺序，保留REFERENCE原项；补项后的时间/缺日降级使用同一份已准备草稿的下标，重新校验完整结果。9项组合测试覆盖这两种相互影响及已认领片段，随后1112项去重解析/身份边界和141项数据库/API通过。

固定原创集r14、r15各24/24、24调用0修复；r16为23/24，最终更正样例仍保留已取消地点、遗漏移日地点。after-r18又复现同一原文片段被同时当作主线与备选。两类错误均保留，没有降低答案。新增校验只要求同次模型修正，不默认选择主线、不按地点名称全局去重，也不在本地推断取消后的完整排程。37项新合成边界覆盖原文确实再访、否定再访、角色归一、取消又恢复、条件、引用及仅取消某日；总计1149项解析/身份边界通过，完整真实复测进行中。

浏览器脚本的runtimeFingerprint只是运行开始的磁盘代码摘要，不是API进程声明；最终复测必须在API重启并返回ready后开始。安全模型摘要没有导出repair_prompt_sha256，缺该字段本身不证明服务版本错误。原文文件摘要与实际trim后的提交摘要分别记录。

取消校验补充Markdown引用和中文单引号边界，只读引用不变成当前最终安排。r19、r20对应的原创集r17、r18各24/24、24调用0修复，失败仍保留在原记录中。附近午饭/午餐/晚餐的纯餐饮推荐名单按原文片段保持REFERENCE；不能因为紧邻13:00就升级为两站，也不覆盖后续明确订位的同名再次出现。20项新合成矩阵覆盖名单、匿名用餐、单家分店、依次串店及附近景点。

后续恢复策略保存第一份仅有时间问题、清除无据时间后再次通过完整原文/角色校验的草稿。仍只给模型一次修正机会；若第二稿失败，返回这份已验证的地点和明确部分结果。失败调用、清除时间数量和恢复自第几稿留在内部，不虚构时间，也不拿有地点遗漏或角色矛盾的草稿兜底。

首稿恢复5项合成测试通过，含有效第二稿优先、首稿有地点/角色错误拒绝恢复、完整再验证失败拒绝恢复；它们是模拟SDK证据，不是实际模型触发恢复的证明。餐饮名单新增7项正反例后共27项通过，保护同句前置订位/串店及“无需订位/尚未选择”。当前1183项去重解析/身份测试通过，141项数据库/API和12项浏览器检查亦通过；固定原创集r19为24/24、24调用0修复。最终同版本复测仍以之后实际页面记录为准。

固定原创集r20为24/24、24调用0修复，测量前后源文件未变。r23未稳定通过，继续保留失败并缩小问题：已有明确加粗并列组补漏仅允许模型保留第一项；现收口到任一唯一的原文成员也可以作为同组依据，保持组内来源顺序，原成员的时间不借给补出的前项。该边界是代码与合成复现发现，r23仅有脱敏缺组错误，不能声称已回读到模型具体漏了哪一个成员。

任一组员恢复通过21项独立合成测试，包含第二/第三项、原成员时间与预约依据、现有其他分类、否定更正、错锚与跨日位置；总计1204项去重解析/身份测试通过。当前继续同版原文验证，不把测试数量当作用户结果。

## 边界

地点未确认、品牌没有分店、真实服务不能唯一匹配时保留待确认；不能为提高显示为已确认的数量降低身份、城市和类别校验。原文票价、预约、营业和交通数字不是本次已验证事实。备选目前按日逐项展示、由用户加入，不自动替用户决定整日方案。

不推送、不合并main、不部署。原第一版工作区及用户数据保留。真人体验、实体手机、Safari、全仓库及生产证据未运行。
