# 已完成：快速提示轮换与可见地点路线（2026-09-07）

> 当前进展：2026-09-07 已修复普通城市的两层搜索限制，以及加粗票种/菜品导致整体失败。真实北京、新余、成都、苏州与新余排版变体均已返回卡片；地点召回、园内明细、备选保留和服务波动尚未达到整体稳定性目标。下一个自主切片应先保留逐项失败原因，区分语义遗漏、名称歧义与服务不可用，再实施有界重试/复用和局部恢复，不能按新增城市逐个硬编码地点。详情以 CURRENT_GOAL.md 最新记录为准。

提示每2.5秒切换；地图按可见已确认地点计算直接邻接，保留原始计划绑定。统一步行≤30分钟，否则选公交，编辑后仍须手动更新。截图实际行程6段路线已完整显示，69项后台与19项前端定向验证通过，生产构建已启动。未增加驾车API或改变匹配范围；未来仍可独立扩充交通方式，当前交付采用用户允许的公交方案。

---

# 已完成：图片多样性与去角标（2026-09-07）

按所有者要求移除图片角标，原九个细分类型各保留10张不同照片，新增81张并保留许可来源。稳定分配和行程内去重已接入主卡片、拖动预览；刷新、重排、重复地点和超过图片池大小的行程均有验证。16项定向测试、类型检查与生产构建通过，用户当前示例页面已实际核对。没有新增模型/地点服务调用，旧地点覆盖边界不变；不扩大到部署。

---

# 已完成：仅展示已确认地点与分类配图（2026-09-07）

所有者当前选择“无候选不显示，已确认可更改”。本轮按此更新生成与结果投影、编辑位置映射、新增搜索、分享和导出；原记录保留，身份校验不放宽。九类网络照片本地化、明确类型配图，并覆盖图片失败及未知类型。最终24项浏览器/客户端、6项后端定向测试和生产构建通过；原文Chrome及3项真实模型/高德案例完成，新建匿名测试数据已清理。

当前攻略显示5张真实卡片，CBD等仍被隐藏；这是显示与失败降级合同完成，不能当作全地点识别覆盖完成。后续可自主改善缺失地点的名称和身份匹配，历史长攻略稳定性问题另保留。当前不处理多份攻略拼接，不扩大到部署。详见[当前状态](CURRENT_GOAL.md)与[本轮交付](CONFIRMED_CARDS_AND_PHOTOS_20260907.md)。以下历史计划不覆盖本次所有者约束。

---

# BreezeTravel 双模式与横链画布实施计划

## 当前：自动地点采用已改善，生成稳定性继续修复（2026-09-07）

新生成行程自动采用经名称、城市、类别和坐标检查的地点，用户保留更换、改城、撤销及顺序调整权利。实际上海示例16个主站中13个匹配，少数歧义继续待确认。906项相关回归、15项持久化/地图/删除检查及13项浏览器检查通过。

本轮不标为整体交付通过：最终同版18次实际多内容/变体/重复测试中16次生成结果，15次主站符合独立预期。第二份上海长攻略两次失败，第二份北京攻略一次多拆，地点服务暂不可用造成匹配数量波动。下一项是原文引用修正、眺望对象与菜名角色的最小复现和修复，再完整复测。状态与[本轮记录](AUTOMATIC_PLACE_CANDIDATES_20260907.md)为准，下面的历史通过不能替代这次失败；不要求所有者排查，也不自动扩大上线范围。

## 已完成：同一攻略纯文字粘贴失败修复（2026-09-07）

针对已给过的上海攻略仍失败，读取当前页面实际765字输入对应的错误：午餐第二处同名街道缺失，失败又占匿名额度。本轮按原文出现位置恢复有限明确备选，保护不同主站/备选各自顺序，保留晚上吃饭街道和馆区限定，修复CRLF说明与餐厅推荐角色；失败不计每日成功额度，处理中并发和隐私删除后的计数仍受约束。

最终同版纯文字、缩进CRLF、Markdown和再次纯文字共4次实际提交，主站16/备选13的名称、日归属、角色和顺序全部符合独立预期；用户原页面最终重试成功，逐日备选可展开查看。1152项解析、46项数据库/API和24/24原创真实模型测量通过。仅4个主站已确认地点身份，12个仍待确认；不将语义符合当作坐标或票价已核验，不扩大本轮到餐宿推荐或上线。当前状态与[修复记录](PLAIN_TEXT_GUIDE_FIX_20260907.md)为准，后续继续待确认地点覆盖与语义稳定性。

## 已完成：七份实际攻略的地点解析修复（2026-09-06～07）

以已交付本地5472927继续隔离强化，处理所有者提交失败及七份明确授权原文。先独立标注逐日主站/备选/顺序，再用真实页面和现有模型/地点服务逐份核对；用户原文仅保留忽略目录供本次复现，不提交或用于长期训练。新增长期回归仍用原创合成文本，原始失败完整保留。

已恢复本地依赖、修复错误地点身份/原文限定/并列/重复与备选、附近餐厅推荐误排主线，以及模型修正损坏此前正确地点的恢复边界。最终同版两轮七份均逐项符合82个主名/46个备选的日期、角色与顺序；1204项解析/身份、141项数据库/API、12项浏览器及最终固定24份真实模型验证通过，本地checkpoint已保存。待确认地点与局部时间降级单独披露，不宣称任意攻略100%准确；本轮不扩大餐饮住宿推荐或部署。实际状态以CURRENT_GOAL和[复现记录](OWNER_GUIDE_REPRODUCTION_20260906.md)为准。

## 当前优先级：地点名词与卡片稳定对应（2026-09-06）

沿用42be8b6已交付版本与隔离工作树。用户明确首要“不漏解、不多解、不少解、不解错”：以独立原创文本逐项核对计划地点的完整名称、出现次数、日归属、顺序与备选角色，覆盖复述、再访、否定、并列、馆区和分店。冻结样本后真实调用当前模型，记录首次失败与修复后同数据结果，不以合成回放冒充模型准确率。真实50地点查询与边界矩阵检验身份匹配；未知专名保留待确认。餐饮住宿扩展暂停，本轮只完成地点搜索城市纠正的已有失败恢复路径。

本轮已完成原文→卡片→真实地点、用户明确纠正→保存／撤销验证，保留首次地图生成和编辑后手动更新约束。最终24/24真实解析样本、487项相关后端测试及真实桌面/手机流程通过；保留待确认与失败边界，不推送上线。完整结果见[解析稳定性交付记录](PLACE_PARSING_STABILITY_DELIVERY_20260906.md)与当前状态；不把定向样本通过率当作所有行程的保证。

## 当前授权：第一版功能强化（2026-09-06）

所有者确认以今天第一版 `a6035de874948029aa593104626c1ac40081d986` 为基线，在 `codex/itinerary-quality-20260906`、`D:/CODEX/BreezeTravel-enhancement` 隔离开发，沿用新版简化约定。本轮覆盖文字保真、行程查验、餐饮与住宿推荐、健壮性及本地交付，替代下面历史切片的“达到 R0 即停止”和“不新增推荐”范围限制。保留今天界面与协同功能；不自动上线。

执行顺序：

1. 文字：真实适配器的时间依据、跨城归属、明确日结构、备选展示及遗漏／重复边界；用合成样例同数据前后测量真实模型。
2. 准确性：住宿排序惩罚与实测耗时分离，餐休不遮蔽路线检查，直接时间重叠、过期／缺失事实保守处理。
3. 可采纳建议：按需展示附近餐饮和住宿，复用真实高德及现有写命令，少量明确理由、候选绑定当前行程、一次采纳且零自动算路。
4. 实用查验：地点、交通、时间、绕行／重复、餐饮与住宿衔接；未采集的营业／预约／天气只披露未核验，不声称通过。检查在现有两视图内按需展开，不恢复前置表单。
5. 集成：相关后端和数据库、真实模型／地图、四视口浏览器、失败恢复与隐私，保存本地 checkpoint 和启动／验收说明。

采用 AGENTS.md 允许的短期子代理处理互不重叠的实现或复核，主任务统一接口并串行集成；不建立功能对话、候选平台或新治理系统。每个问题至多两轮不同修复，保留实际失败。新样例不得使用用户原始攻略或历史 blind 数据。真实验证仅使用既有授权服务与合成测试文本，记录次数与实际结果。

产品合同见 [PROJECT_CHARTER](../product/PROJECT_CHARTER.md)，当前进度见 [CURRENT_GOAL](CURRENT_GOAL.md)。实施基线为长攻略修复检查点 `a43b1a8`；旧 G07 与旧 R0 只保留为历史。

## 串行切片

1. **渐进生成与停止**：扩展 202 与 SSE 公开投影、累计脱敏快照、资源级游标、事件流优先／轮询降级、取消竞争与迟到 worker 防写回。
2. **两视图横链画布**：接回现有横链／导航供体，以当前 Hook、真实 `RouteMap`、编辑和预览面板为权威；共享横滚、固定日期、弧线、手机列表模式。
3. **拖动、预演与导出**：图片或把手拖动即时预览、相邻卡片让位，松开一次写入；键盘保留确认；真实几何客户端预演；完整无底图 PNG。
4. **协同运行时与换肤**：精确挂载旧链所需 API，收口公开投影与假成功，恢复房间、Yjs、四节点 AI、明确触发驾车排线及本地／Compose 生命周期。
5. **单向转换**：读取当前成员最新成功保存路线，白名单规范成文字，经 v3 重新理解核验，保持房间不变。
6. **集成验收**：先完成离线回归、固定数据闭环、生产构建和四视口验证；达到自动化 R0 候选后立即交付所有者三份文字攻略体验。真实 Qwen／高德与真实协同 AI／驾车属于 R1 验证，不阻塞 R0 体验。

同一时间只集成一个切片。子 Agent 只复核与诊断，产品代码由主任务串行写入。每个切片先有用户可见闭环，再补最小必要测试；一个问题最多两种不同修复策略。

## 接口增量

- `GET /api/v3/trip-understandings/{id}/result` 的 202 增加 `phase`、`event_cursor`、`progress` 和可选脱敏 `snapshot`。
- 现有 SSE `progress` 增加相同阶段、计数和累计快照；终态仍为 `result_available`。
- `POST /api/v3/trip-understandings/{id}/cancel` 要求 `Idempotency-Key`，返回 `STOPPED_WITH_DRAFT / STOPPED_EMPTY / ALREADY_FINISHED`。
- `POST /api/v3/trip-understandings/from-collaboration` 请求仅 `{room_id}`，要求登录与幂等键，返回现有 202 accepted view。
- 追加 migration 为理解任务、资源和修订增加 `CANCELLED`，不新增公开 job ID 或导出表。

## 分级交付

| 等级 | 交付含义 | 最小结果 |
|---|---|---|
| R0 | 所有者本地体验 | 40 条无危险输出且至少 28 条自动符合；3/3 攻略返回卡片或诚实部分结果，至少两份经少量编辑可用，一份完成卡片→编辑→路线需更新→手动更新→PNG；协同固定闭环、停止、重连和预演可操作 |
| R1 | 封闭受控试用 | 至少 36/40；真实 Qwen＋高德行程查一次、真实协同 AI＋驾车一次；双用户同步、重开和转入行程查成功 |
| R2 | 新版候选 | 40/40 精确通过，完整响应式、无障碍、Provider 和候选矩阵通过 |

R0/R1 是有效成果但不称 G07 通过。普通样式和边缘识别可记录降级；地点身份、隐私、删除、自动算路、未知冒充成功与推测路线失败时必须关闭能力或诚实受限。

## 必测范围

- 现有 v3、认证、地图、住宿、检查后端、分享和隐私安全不得回退。所有者明确移除独立优先检查页面后，5 条仅适用该页面的浏览器场景保留为 RETIRED_CHECKS_PAGE，不计入通过数量；保留读写并发、失败恢复、内部信息不泄漏等验证。
- SSE：首次、续订、多行／坏帧、重复倒退事件、资源隔离、旧任务迟到、轮询单飞降级。
- 取消：有／无快照、幂等重放、完成竞争、取消 worker 迟到提交。
- 横链：单／双／多卡、空日、响应式回环折行、同日／跨日移动、取消零写、确认一次、键盘移动、路线失效和手动更新。
- 预演零网络；PNG 包含视口外全部日期和待确认／过期／受限标记，ETag 变化不下载。
- 协同：登录、创建／加入、双客户端 Yjs、三类 AI 进度、显式排线、真实驾车或诚实不可用、五分钟令牌刷新。
- 转换：非成员、无／空／坏保存路线、幂等、字段白名单、新行程归属、旧房间零修改。
- 1440、1280、390、360px 与前端生产构建。

## 停止点

达到 R0 后立即交付所有者真人体验，不等待完整候选。生产部署、公开发布、`main` 合并、招募 H1、付费或新外部数据权限均不在本目标授权内。

## 当前串行切片：所有者界面反馈

从已交付双模式检查点 91e92e4 建立 codex/soft-canvas-owner-feedback，工作树 D:/CODEX/BreezeTravel-soft-canvas。保留旧工作树与用户 AGENTS 修改。此切片顺序：极简首页和真实阶段 → 拖拽与全日期地图 → POI 照片 → 恢复/触摸/四视口与生产构建。实际结果单独记录，不拼接旧候选结论。

本次新增照片可空字段保持旧客户端兼容，同步当前 OpenAPI 与客户端。拖拽验收更新为“悬停零写入、松开一次、取消零写入、删除区删除”，键盘保留确认对话框；其余安全和隐私门槛不变。

## 第二轮界面反馈：两视图与就地操作

本切片从本地检查点 `967414e` 继续，不重建产品或合并旧仓库。只保留卡片／地图两个视图；地图首屏完整、日期颜色图例在下方，路线与住宿收进就近小浮层。行程信息在原下拉框内修改。生成过程增加漂浮纸片、地点扫描与完成动效，由实际进度驱动，不虚构阶段进度。

验收：1440、1280、390、360px 地图边界均在首屏内；照片和把手均能同日／跨日移动，落点预览与卡片让位可见，快速松手位置正确；悬停／取消零写、松手一次、编辑不算路；信息编辑无远距离滚动，焦点可恢复；停止、重连、导出、隐私与错误恢复继续可用。状态以 CURRENT_GOAL 本轮结果为准。

## 第三轮界面反馈：紧凑回环与相对顺序

从本地检查点 `a6ec335` 继续。此条覆盖此前重力锁链／单行横向滚动要求：卡片按容器宽度自动排为 1–6 列，目标桌面 4–6 列、360/390px 手机两列；一行从左到右，下一行从右到左。序号和 DOM 保持真实逻辑顺序，顶部曲线和行末转弯只表达先后关系，绝不冒充地图路线。交通摘要仍来自当前服务端结果。

卡片、详情、编辑、临时卡片和导出不再要求到访时间或停留时长。保留既有字段与存量数据，不修改公开合同、数据库或后端核验。只隐藏这些时间展示与编辑；步行／公交的实际耗时继续显示，未确认和过期路线继续诚实标记。

验收新增：13 卡长日跨三行的正确顺序；四视口无横向溢出；第二行反向拖入逻辑位置正确；跨日让位与取消／单次写入保留；完整 PNG 覆盖视口外卡片、反向行、待确认和路线过期且不含到访时间。旧重力曲线／200px 横向占位断言改成当前布局断言；原时间编辑的超时重放、路线失效场景改为文字编辑，保留幂等、一次应用、并发锁与零自动路线请求检查，不新增跳过。

## 第四轮界面反馈：外侧连线、局部下拉与可读提示

从 `a91d86f` 开始，串行实施外侧两行回环 → 步行距离／时间与就近手动更新 → 待确认卡片内搜索确认 → 舒缓生成提示。PNG 共用同一坐标与交通事实选择逻辑，公共 API、数据库和 Provider 配置不变。

验收：四视口上排标签在上方、下排在下方、右侧折返且无横向溢出；保留占位、移除蓝色竖条；三视口非模态下拉就近搜索／确认、打开零请求、确认一次写入、失败可重试和 Escape 焦点恢复；步行优先且距离真实，过期零旧时长、更新只在点击后发生；提示至少九秒轮换、悬停或聚焦可停留阅读，尊重减少动态效果；既有隐私、SSE、撤销／并发和导出回归不退化。

## 第五轮：统一地点修改

从 `cdaec59` 继续，短导航／占位文案 → 全部卡片统一搜索 → 地图内就近修改 → 键盘等价拖动 → 回归与本地构建。旧文字编辑测试改为真实候选确认路径，保留超时重放、单次应用、并发锁、过期路线与零自动算路断言；旧键盘面板测试改为键盘提起／跨日／放下／取消，不降低安全检查。

验收新增四视口已确认卡片与地图地点修改，无模态遮罩、无旧编辑工作区、无页面跳转；候选选择零写、确认单次写、地图不自动重算。桌面导航高度只容纳两个按钮，首页只有输入框内的操作提示。

## 第六轮：地图空间与同行确认

从 `0be5dfe` 继续。先收起成功提示并合并地图操作栏，再将候选确认放进被选中的同行，支持再次点击确认。四视口验证地图紧贴标题、首屏底部可见、按钮随候选出现；验证改选零写、重复点击单次写、错误恢复与旧幂等回放。生产构建后复跑现有真实文字／地图闭环，不操作所有者存量行程。

## 2026-09-06 首次服务器上传授权

所有者明确批准：以当前 2c8d9b0 为起点，进行真实完整流程测试，并按本机服务器访问包将最新版更换到现有服务器。此授权覆盖该服务器的首版发布，替代此前不部署的本轮边界；不授权合并 main、改写历史、删除旧业务数据、动同机其他服务或新增费用。

顺序：安全读取访问包并核对指纹 → 真实文字／地图／编辑／保存／分享删除及协同核心闭环 → 检查服务器当前实例、配置名称与备份 → 独立新发布目录和回滚点 → 构建及私有端口验证 → 切换已有站点 → HTTPS 实测与状态记录。发现安全或核心流程失败先修复；不能以固定回放声称真实验证通过。凭据不进入 Git、聊天或测试报告。
## 当前优化：先解决地点匹配（2026-09-06）

从 c6bea8f 开始，依据所有者新要求，先测量常见地点真实匹配和调用次数，再修名称衔接、主地点类别与手动候选排序。保留现有有界并发，不增加模型调用、不扩大模糊自动确认、不改 v3/API/数据库。验收包括主场馆不被子活动替代、同名歧义/错城/错类别继续待确认，以及真实文字至卡片和搜索确认闭环。先完成本地检查点，不把首次上传授权扩展为每次自动部署。

后续产品项：在路线基本可信后，考虑按现有行程上下文给出一项餐饮建议、一项住宿建议；采用按需、可忽略、无虚构价格/房态的形式。既有住宿功能保留，本轮不新增建议入口。
