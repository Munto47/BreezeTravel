global:
  scrape_interval: 5s

scrape_configs:
  - job_name: breezetravel-backend
    metrics_path: /metrics/prometheus
    static_configs:
      - targets: ["backend-a:8000", "backend-b:8000"]
        labels:
          environment: local-controlled-multi
